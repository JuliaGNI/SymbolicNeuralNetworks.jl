var documenterSearchIndex = {"docs":
[{"category":"section","location":"limitations/#Limitations","page":"Limitations","text":"The assumptions and rough edges of this package, collected in one place.","title":"Limitations"},{"category":"section","location":"limitations/#Code-generation-dominates-the-cost","page":"Limitations","text":"Everything here is built once and evaluated many times, and the build is the expensive half. Two things make it so.\n\nThe generated code re-emits a subexpression once per use, because Symbolics stores an expression as a hash-consed graph but prints it as a tree. Common subexpression elimination — on by default, see Building Functions — keeps that proportional to the number of distinct operations instead.\n\nThe expression itself grows with the network when a whole composition is written down as one formula: a Chain's forward pass inlined layer into layer is exponential in depth. For the pullback this is avoided by not inlining it, which is what SymbolicPullback does by default; see How the pullback is built. Elsewhere — Jacobian, Gradient, build_nn_function of an expression over the whole model — the single expression is still what is built, so those remain practical for small networks only.\n\nscripts/codegen_comparison.jl measures both effects.","title":"Code generation dominates the cost"},{"category":"section","location":"limitations/#The-default-result-cannot-be-differentiated-by-Zygote","page":"Limitations","text":"By default a batch is evaluated by an in-place kernel that mutates a preallocated array, which Zygote does not support (Mutating arrays is not supported). This matters whenever a generated function is used inside a loss that is then differentiated in reverse mode.\n\nPass inplace = false to build_nn_function for the out-of-place path, which is differentiable at the cost of one array per sample. Forward-mode AD (ForwardDiff) works with either path.","title":"The default result cannot be differentiated by Zygote"},{"category":"section","location":"limitations/#The-element-type-comes-from-the-inputs","page":"Limitations","text":"The in-place kernels write into an array that has to be allocated before they run, so its element type is promoted over the inputs (promoted_eltype) rather than taken from the result.\n\nTwo consequences:\n\nAn equation over integer inputs and integer parameters does not generally evaluate to an integer, so an integer element type is widened with float.\nA Float32 network whose generated code contains a Float64 literal produces Float32, i.e. the literal is rounded rather than the result widened. That is the behaviour one wants for a network, but it is worth knowing about.","title":"The element type comes from the inputs"},{"category":"section","location":"limitations/#SymbolicPullback-assumes-the-loss-is-additive-over-the-batch","page":"Limitations","text":"SymbolicPullback differentiates the loss of a single sample and sums the per-sample gradients over the batch (reduce = +). That is the gradient of the batched loss exactly when the loss is a sum over samples.\n\nAbstractNeuralNetworks.FeedForwardLoss is not: it normalises by norm(output) taken over the whole batch, so for a batch of more than one sample the symbolic pullback computes the gradient of the summed per-sample losses, which differs from the gradient of the batched loss. For training this is a perfectly reasonable objective — and it is what the per-sample formulation means — but it is not identical to what a Zygote-based pullback of the same NetworkLoss returns.\n\nThe same loss also divides by norm(output), so a target that is identically zero gives NaN.","title":"SymbolicPullback assumes the loss is additive over the batch"},{"category":"section","location":"limitations/#Batch-shapes","page":"Limitations","text":"All data arguments of a generated function must have the same number of dimensions and the same batch size, however many of them there are.\nA matrix-valued equation cannot be evaluated on a batch with two batch dimensions when reduce = hcat: concatenating the per-sample results already uses the second dimension. Use reduce = +, or reshape the input into a matrix.\nAbstractNeuralNetworks.Dense computes ps.W * x, which has no method for a three-dimensional x. A generated function handles such an input, but the model it came from cannot be called on one, which is why references in the test suite are assembled sample by sample. See issue #40.","title":"Batch shapes"},{"category":"section","location":"limitations/#What-a-layer-carries-alongside-the-state","page":"Limitations","text":"The layerwise construction of SymbolicPullback puts a seam between two layers, and by default that seam is one plain vector — so it assumes every layer maps an array to an array. A layer that carries something else alongside the state can say so (seam_interface), within limits:\n\nthe carried data must reach the generated kernels as an array with the same rank and batch size as the state, like every other data argument. For data that is the same for the whole batch that means broadcasting it out to one column per sample.\na carried datum that varies per sample cannot be combined with a state that has two batch dimensions: batched lays the state out flat and leaves the carried part alone, so the two would no longer correspond.\nthe chain's last layer has to return the model's output and nothing beside it, since that is what the loss and its seed compare against the target.\na layer that carries nothing declares (), not an empty array.\nthe carried variables must be named something of their own. x is the state and λ the sensitivities, and reusing either name gives the same symbolic array a second slot rather than a second array; the construction refuses such a seam rather than generating kernels for it.\n\nA layer that carries something and declares none of this makes the construction decline, and the monolithic path takes over. That path traces the chain from a plain vector, so a layer which defaults what it carries — as GeometricMachineLearning's SymplecticEuler defaults the parameters of the system to NullParameters — is differentiated with that default, whatever the caller passes. The gradient is then right for the default-carried map and for no other.","title":"What a layer carries alongside the state"},{"category":"section","location":"limitations/#Reserved-names","page":"Limitations","text":"The generated kernels call their own arguments out, ps, k, and x1, x2, … — one per data argument, of which there may be any number.\n\nThe whole x-followed-by-a-number family is reserved and not merely the arities an equation happens to use, so that a free variable named x3 is rejected today rather than the day a third data argument arrives.\n\nA symbolic variable that is passed to build_nn_function — as a data variable or as part of the parameters — may be named anything: Symbolics.build_function turns it into an argument and renames it. Only a variable left free in the equation, i.e. one that is neither, survives into the generated code under its own name. If that name is one of the five above it would be bound by the kernel's own argument, so it is rejected with an error when the function is built.","title":"Reserved names"},{"category":"section","location":"limitations/#Upstream-code-generation","page":"Limitations","text":"The rewrite rules in src/codegen/expression_rewriting.jl rely on properties of the code Symbolics.build_function emits which are not part of its documented interface. They are asserted directly by test/codegen/codegen_drift.jl, so a change upstream fails there with a clear message rather than surfacing as a confusing downstream error. See Code Generation.","title":"Upstream code generation"},{"category":"section","location":"guide/training/#Training-a-Symbolic-Neural-Network","page":"Training","text":"SymbolicPullback is a drop-in replacement for a Zygote-based pullback, so a SymbolicNeuralNetwork can be trained with the optimizers of GeometricMachineLearning without any further ceremony.\n\nWe approximate a Gaussian on -1 1times-1 1 with a small feed-forward network.\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, params, FeedForwardLoss\n\nc = Chain(Dense(2, 3, tanh), Dense(3, 3, tanh), Dense(3, 1, tanh))\nsnn = SymbolicNeuralNetwork(c)\npb = SymbolicPullback(snn)\nnothing # hide\n\nSymbolicPullback(snn) uses a FeedForwardLoss; pass a NetworkLoss as the second argument for anything else.","title":"Training a Symbolic Neural Network"},{"category":"section","location":"guide/training/#How-the-pullback-is-built","page":"Training","text":"The gradient a SymbolicPullback evaluates is fixed; how it is constructed is not, and the difference decides which networks it can be built for at all.\n\nThe direct route is to write down one symbolic expression for the loss of the whole network and differentiate it once per scalar parameter. Both halves of that grow badly. A Chain's forward pass is inlined layer into layer, so layer k's expression contains layer k-1's once per element it reads and the loss expression is O(mathrmwidth^mathrmdepth) before anything is differentiated; differentiating it then walks the whole of it once per parameter. Four layers of width 16 — 626 parameters — reach a gradient expression of 2·10⁸ nodes, and the build never finishes.\n\nCommon subexpression elimination does not help with this. It runs at code generation, on an expression that has already been built and traversed; see Building Functions.\n\nThe way out is to stop inlining the composition. Each layer gets fresh symbolic variables for its own input, so the expressions built for it refer to that layer and to nothing upstream of it, and the composition happens when the pullback is evaluated — where it costs a function call per layer. Per layer this differentiates the scalar\n\ns_k = lambda_k cdot f_k(x_k-1 theta_k)\n\ntwice, in which lambda_k stands for the sensitivity of the loss to that layer's output: partials_kpartialx_k-1 is lambda_k-1, and partials_kpartialtheta_k is partialLpartialtheta_k. Neither a Jacobian nor a rank-3 parameter derivative is ever materialised, and the total symbolic material becomes a sum over layers rather than a product:\n\nlayers width parameters monolithic layerwise\n2 4 22 6 652 792\n4 4 62 388 700 2 520\n6 4 102 12 848 828 4 248\n4 8 186 8 253 148 11 736\n4 16 626 209 455 964 68 760\n\nCounted in expression nodes by scripts/codegen_comparison.jl, which also times both constructions.\n\nThis is what SymbolicPullback does by default. The layerwise keyword overrides the choice:\n\nSymbolicPullback(snn, loss; layerwise = true)    # demand it, error if it does not apply\nSymbolicPullback(snn, loss; layerwise = false)   # one expression for the whole network\n\n:auto, the default, composes layer by layer for every model that decomposes into more than one layer — see composes_layerwise for the measured crossover and why the default does not try to reproduce it exactly.","title":"How the pullback is built"},{"category":"section","location":"guide/training/#Layers","page":"Training","text":"What sits between two layers is a seam: fresh symbolic variables standing for the layer's input, rather than the expression of everything upstream of it. By default that is one plain vector — the layer's state — because that is what a Dense maps to a Dense.\n\nA layer may carry more than the state. GeometricMachineLearning's SymplecticEuler threads the parameters of the system through the chain alongside it, so it takes and returns a pair. Four functions say how such a layer meets the seam, and each defaults to the plain-vector construction:\n\nfunction what it answers\ncarried_variables what fresh variables the carried data needs\nseam_value what the layer is applied to at the seam\nstate_expressions which part of the output lambda pairs with\nseam_arguments the run-time arguments of the generated kernels\n\nSymbolicNeuralNetworks.carried_variables(l::MyLayer) = (Symbolics.variables(:c, 1:length(l)),)\nSymbolicNeuralNetworks.seam_value(::MyLayer, sx, sc) = (sx, sc)\nSymbolicNeuralNetworks.state_expressions(::MyLayer, y) =\n    SymbolicNeuralNetworks.scalar_expressions(first(y))\nSymbolicNeuralNetworks.seam_arguments(::MyLayer, x::Tuple) = (first(x), last(x))\n\nA layer declares all four together or none of them. The carried data is data, never a differentiation target: lambda pairs with the state, the seed is differentiated with respect to the state and the layer's parameters, and the carried variables become extra arguments of the generated kernels. See seam_interface.\n\nA layer that carries something and has not declared these is where the construction declines — its output has more in it than the default can take apart, or it has no method for a bare vector at all — and SymbolicPullback falls back to the monolithic path. That fallback traces the chain from a plain vector, so it differentiates the map in which every layer defaulted what it carries: for a model whose carried data matters, declaring the seam is not only faster but the only construction that is right.","title":"Layers"},{"category":"section","location":"guide/training/#Losses","page":"Training","text":"The layerwise construction needs one thing the monolithic one does not: the loss as a function of the network's prediction, so that the sweep has a value of partialLpartialhaty to start from. AbstractNeuralNetworks has no interface for that — a NetworkLoss is applied as loss(model, ps, input, output) — so the expression is obtained by applying the loss to a PassThroughLayer, a model whose prediction is its input.\n\nThat is right for a loss which reaches its model once and compares the result to output, and wrong for one that does something else: an autoencoder loss compares the prediction to the network's input, and so reads through a pass-through model as identically zero. Returning a zero gradient quietly would be the worst thing this could do, so the guessed expression is checked against the loss itself before it is used, and the construction falls back to the monolithic one when the two disagree.\n\nA loss can say what its expression is instead, in which case it is used as given:\n\nSymbolicNeuralNetworks.loss_expression(::MyLoss, ŷ, y) = ...\n\nSee loss_expression.","title":"Losses"},{"category":"section","location":"guide/training/#The-data","page":"Training","text":"using GeometricMachineLearning\n\nx_vec = -1.0:0.1:1.0\ny_vec = -1.0:0.1:1.0\nxy_data = hcat([[x, y] for x in x_vec, y in y_vec]...)\nf(x::Vector) = exp.(-sum(x .^ 2))\nz_data = mapreduce(i -> f(xy_data[:, i]), hcat, axes(xy_data, 2))\n\ndl = DataLoader(xy_data, z_data)\nnothing # hide\n\nusing CairoMakie\n\nfig = Figure()\nax = Axis3(fig[1, 1])\nsurface!(x_vec, y_vec, [f([x, y]) for x in x_vec, y in y_vec]; alpha = .8, transparency = true)\nfig","title":"The data"},{"category":"section","location":"guide/training/#Training","page":"Training","text":"import Random # hide\nRandom.seed!(123) # hide\nnn_cpu = NeuralNetwork(c, CPU())\no = Optimizer(AdamOptimizer(), nn_cpu)\nn_epochs = 1000\nbatch = Batch(10)\no(nn_cpu, dl, batch, n_epochs, pb.loss, pb; show_progress = false); # hide\n@time o(nn_cpu, dl, batch, n_epochs, pb.loss, pb; show_progress = false);\nnothing # hide\n\nfig = Figure()\nax = Axis3(fig[1, 1])\nsurface!(x_vec, y_vec, [c([x, y], params(nn_cpu))[1] for x in x_vec, y in y_vec];\n         alpha = .8, colormap = :darkterrain, transparency = true)\nfig","title":"Training"},{"category":"section","location":"guide/training/#Comparison-with-a-Zygote-based-pullback","page":"Training","text":"The same training run with GeometricMachineLearning.ZygotePullback:\n\npb2 = GeometricMachineLearning.ZygotePullback(FeedForwardLoss())\no(nn_cpu, dl, batch, n_epochs, pb2.loss, pb2; show_progress = false); # hide\n@time o(nn_cpu, dl, batch, n_epochs, pb2.loss, pb2; show_progress = false);\nnothing # hide\n\ninfo: Info\nFor a plain feed-forward loss like this one there is no speed-up to be had — Zygote handles it perfectly well. The case for SymbolicNeuralNetworks is losses that contain a derivative of the network, such as those of Hamiltonian neural networks, where reverse-mode AD has to differentiate through a derivative; see Double Derivatives.","title":"Comparison with a Zygote-based pullback"},{"category":"section","location":"guide/derivatives/#Derivatives","page":"Derivatives","text":"There are two directions in which a neural network can be differentiated, and this package has a struct for each:\n\nJacobian differentiates with respect to the input,\nGradient differentiates with respect to the parameters.\n\nBoth store a symbolic expression, which derivative returns and build_nn_function compiles. Because the result of one is again a symbolic expression, they compose freely — see Double Derivatives.\n\nusing SymbolicNeuralNetworks\nusing SymbolicNeuralNetworks: Jacobian, Gradient, derivative\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 3, tanh), Dense(3, 2, tanh))\nsnn = SymbolicNeuralNetwork(c)\nnn = NeuralNetwork(c)\nps = params(nn)\nnothing # hide","title":"Derivatives"},{"category":"section","location":"guide/derivatives/#Jacobian","page":"Derivatives","text":"Jacobian differentiates an expression with respect to nn.input. Without an expression it takes the output of the network:\n\nj = Jacobian(snn)\nsize(derivative(j))\n\nThe convention is square_ij = partialf_ipartialx_j, so the result is mathrmoutput_dimtimesmathrminput_dim — the same convention Zygote and ForwardDiff use:\n\nimport ForwardDiff\n\ninput = rand(2)\njacobian = build_nn_function(derivative(j), snn)\njacobian(input, ps) ≈ ForwardDiff.jacobian(x -> c(x, ps), input)\n\nAn expression can also be given explicitly, in which case it is flattened with vec first, so the rows of the result are indexed by vec(f):\n\nJacobian(c(snn.input, params(snn)) .^ 2, snn) |> derivative |> size","title":"Jacobian"},{"category":"section","location":"guide/derivatives/#Gradient","page":"Derivatives","text":"Gradient differentiates with respect to params(snn). Its result has the shape of the parameters: for each entry of the differentiated expression there is one full parameter set holding the derivative with respect to each parameter.\n\ng = Gradient(snn)\nderivative(g)[1].L1.b\n\ninfo: Terminology\nThe name Gradient is not used in the usual sense here. A gradient normally collects the partial derivatives of a scalar function; Gradient differentiates every entry of an array with respect to every parameter, so the gradient of a matrix is a matrix of parameter sets:mathttGradientleft( beginpmatrix m_11  cdots  m_1m  vdots  vdots  vdots  m_n1  cdots  m_nm endpmatrix right) = beginpmatrix nabla_mathbbPm_11  cdots  nabla_mathbbPm_1m  vdots  vdots  vdots  nabla_mathbbPm_n1  cdots  nabla_mathbbPm_nm endpmatrixwhere mathbbP are the parameters of the network.\n\nThe underlying function is symbolic_parameter_gradient, which can also be used directly. For a scalar expression it returns a single parameter set rather than an array of them:\n\nusing SymbolicNeuralNetworks: symbolic_parameter_gradient\nusing LinearAlgebra: norm\n\ngradient = symbolic_parameter_gradient(norm(c(snn.input, params(snn))) ^ 2, snn)\nkeys(gradient)\n\nSuch a parameter-shaped expression is an equation set, which build_nn_function builds as a single function whose result has the same nesting. Summing over the batch with reduce = + gives the gradient of the summed per-sample expression:\n\nf = build_nn_function(gradient, params(snn), snn.input; reduce = +)\nf(rand(2, 8), ps).L1.b","title":"Gradient"},{"category":"section","location":"guide/derivatives/#SymbolicPullback","page":"Derivatives","text":"SymbolicPullback packages that up for training: it differentiates an AbstractNeuralNetworks.NetworkLoss with respect to the parameters and presents the result the way an optimizer expects a pullback to look.\n\nusing AbstractNeuralNetworks: FeedForwardLoss\n\npb = SymbolicPullback(snn, FeedForwardLoss())\n\ninput, output = rand(2, 8), rand(2, 8)\nloss_value, pullback = pb(params(nn), c, (input, output))\nloss_value\n\nThe second entry is a function of the output sensitivities. A NetworkLoss is scalar-valued, so those are just 1:\n\npullback(1).L1.b\n\nConstructing the pullback is where most of the time goes; evaluating it is fast and allocates once per call. See Training a Symbolic Neural Network for a complete training run, and Limitations for the assumption SymbolicPullback makes about the loss.","title":"SymbolicPullback"},{"category":"section","location":"guide/symbolic_neural_networks/#Symbolic-Neural-Networks","page":"Symbolic Neural Networks","text":"A SymbolicNeuralNetwork pairs a model with symbolic stand-ins for its parameters and its input. Everything else in this package is built from expressions formed with those two.\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, params\n\nc = Chain(Dense(2, 3, tanh), Dense(3, 1, tanh))\nsnn = SymbolicNeuralNetwork(c)\n\nAny of the following can be used to construct one — a model, an architecture, a single layer, or an AbstractNeuralNetworks.NeuralNetwork whose numeric parameters are only used for their shapes:\n\nSymbolicNeuralNetwork(c)                       # a Chain\nSymbolicNeuralNetwork(Dense(2, 3, tanh))       # a single layer, wrapped in a Chain\nSymbolicNeuralNetwork(architecture)            # an Architecture\nSymbolicNeuralNetwork(architecture, model)\nSymbolicNeuralNetwork(NeuralNetwork(c))","title":"Symbolic Neural Networks"},{"category":"section","location":"guide/symbolic_neural_networks/#The-symbolic-input-and-parameters","page":"Symbolic Neural Networks","text":"The input is a vector of symbolic variables, one per input dimension:\n\nsnn.input\n\nThe parameters have the same nesting as the numeric ones — one entry per layer, each holding the weight matrix and the bias vector — with every leaf replaced by symbolic variables of the same shape:\n\nparams(snn).L1.W\n\nThey are numbered in the order in which they occur in the parameter set, so W_1 is the weight matrix of the first layer, W_2 its bias, W_3 the weight matrix of the second layer, and so on. This is done by symbolic_variables, which works on any nesting of NamedTuples and NetworkParameters.\n\ninfo: These are arrays of scalar variables, not symbolic arrays\nparams(snn).L1.W is a Matrix{Num}, not a Symbolics.Arr. The difference matters: Symbolics cannot differentiate with respect to an entry of a Symbolics.Arr without scalarising it first, and Symbolics.build_function cannot generate code for an expression that still contains one. Using scalar variables throughout avoids both problems, at the cost of expressions that are fully expanded — which is why this package is meant for small networks.","title":"The symbolic input and parameters"},{"category":"section","location":"guide/symbolic_neural_networks/#Building-expressions","page":"Symbolic Neural Networks","text":"Applying the model to the symbolic input and parameters gives the symbolic output:\n\nsoutput = c(snn.input, params(snn))\n\nFrom there, ordinary Julia code builds whatever expression is wanted:\n\nusing LinearAlgebra: norm\n\nnorm(soutput) ^ 2\n\nReductions such as sum and norm work directly, because the output is an ordinary array of scalar expressions.\n\nExpressions may also involve variables of your own — a target output, for instance, which is what a loss function needs:\n\nusing Symbolics\n\nstarget = Symbolics.variables(:y, 1:1)\n(soutput - starget) .^ 2\n\nAny expression built this way can be turned into a function with build_nn_function; see Building Functions.","title":"Building expressions"},{"category":"section","location":"guide/symbolic_neural_networks/#Printing-expressions","page":"Symbolic Neural Networks","text":"Symbolic expressions get long quickly. Latexify renders them readably:\n\nusing Latexify: latexify\n\nlatexify(soutput[1])","title":"Printing expressions"},{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"constant","location":"api/#SymbolicNeuralNetworks.BATCH_INDEX","page":"API","text":"BATCH_INDEX\n\nThe name the generated kernels give the index of the batch column they evaluate. See index_by_batch.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.BATCH_INDEX"},{"category":"type","location":"api/#SymbolicNeuralNetworks.EquationSet","page":"API","text":"EquationSet\n\nAn arbitrarily nested NamedTuple or NetworkParameters.\n\nThis is the shape of the parameters of a neural network, and therefore also the shape of a symbolic derivative with respect to them. build_nn_function builds a whole set of equations of this shape as one function; see flatten_equations.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.EquationSet"},{"category":"constant","location":"api/#SymbolicNeuralNetworks.FIXED_NAMES","page":"API","text":"FIXED_NAMES\n\nThe three names a generated kernel always gives its own arguments, whatever its arity. See is_reserved_name for the rest.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.FIXED_NAMES"},{"category":"constant","location":"api/#SymbolicNeuralNetworks.OUTPUT_NAME","page":"API","text":"OUTPUT_NAME\n\nThe name the generated in-place kernels give the array they write into. See accumulate_into_output.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.OUTPUT_NAME"},{"category":"constant","location":"api/#SymbolicNeuralNetworks.PARAMETER_NAME","page":"API","text":"PARAMETER_NAME\n\nThe name the generated kernels give their parameter argument. The rewrite rules turn every parameter argument of the code Symbolics.build_function emits into a getproperty on it, so that the kernel can be called with a single NetworkParameters.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.PARAMETER_NAME"},{"category":"type","location":"api/#SymbolicNeuralNetworks.SymbolicExpression","page":"API","text":"SymbolicExpression\n\nEverything this package accepts as an equation: a single symbolic expression or an array of them.\n\nEquations are built by applying a model to symbolic variables, e.g.\n\nc(nn.input, params(nn))\n\nBoth representations Symbolics offers are covered. This package itself only ever produces arrays of scalar expressions (see symbolic_variables), but a Symbolics.Arr handed in by a user is accepted as well and normalised by scalar_expressions.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.SymbolicExpression"},{"category":"type","location":"api/#SymbolicNeuralNetworks.SymbolicVariables","page":"API","text":"SymbolicVariables\n\nA symbolic array that a generated function takes as a data argument: the network input, or the target output of a loss.\n\nBoth the Vector{Num} that SymbolicNeuralNetwork builds and the Symbolics.Arr that @variables x[1:n] produces are accepted; the latter is scalarised by scalar_expressions.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.SymbolicVariables"},{"category":"method","location":"api/#AbstractNeuralNetworks.FeedForwardLoss-Tuple{Union{AbstractNeuralNetworks.AbstractExplicitLayer, AbstractNeuralNetworks.Chain}, NeuralNetworkParameters.NetworkParameters, Union{Symbolics.Num, SymbolicUtils.BasicSymbolicImpl.var\"typeof(BasicSymbolicImpl)\"{T} where T, AbstractArray{Symbolics.Num}, AbstractArray{<:SymbolicUtils.BasicSymbolicImpl.var\"typeof(BasicSymbolicImpl)\"{T} where T}}, Union{Symbolics.Num, SymbolicUtils.BasicSymbolicImpl.var\"typeof(BasicSymbolicImpl)\"{T} where T, AbstractArray{Symbolics.Num}, AbstractArray{<:SymbolicUtils.BasicSymbolicImpl.var\"typeof(BasicSymbolicImpl)\"{T} where T}}}","page":"API","text":"(::FeedForwardLoss)(model, params, input, output)\n\nThe FeedForwardLoss of AbstractNeuralNetworks, evaluated on symbolic arguments.\n\nwarning: Zero targets give `NaN`\nThe loss is normalised by norm(output), so a target that is identically zero makes the generated function return NaN/Inf.\n\n\n\n\n\n","title":"AbstractNeuralNetworks.FeedForwardLoss"},{"category":"type","location":"api/#SymbolicNeuralNetworks.AbstractBatchedFunction","page":"API","text":"AbstractBatchedFunction{NDATA, R}\n\nAn executable function built from a symbolic equation, as returned by build_nn_function.\n\nIt wraps a kernel that evaluates a single sample (see build_kernel and build_kernel!) and adds everything the kernel does not do: iterating over a batch, allocating and shaping the result, and accepting a single sample or a three-dimensional batch instead of a matrix.\n\nNDATA is the number of data arguments, so an instance is called as\n\nf(input, ps)            # NDATA = 1\nf(input, output, ps)    # NDATA = 2\nf(x1, …, xNDATA, ps)    # in general\n\nand R is how the per-sample results are combined — hcat or +.\n\nThere is no bound on NDATA. One data argument is the network input, a second is typically the target output of a loss, and the layerwise pullback uses one per entry of a layer's seam plus one for the output sensitivities — see seam_interface.\n\nResult shapes\n\nFor an equation of size (m n ldots) and a batch of N samples:\n\ndata arguments R result\nvectors either the shape of the equation\ndtimesN matrices hcat mtimes(ncdotldotscdotN)\ndtimesN matrices + the shape of the equation\ndtimesN_1timesN_2 arrays hcat mtimesN_1timesN_2 (vector- or scalar-valued equations only)\ndtimesN_1timesN_2 arrays + the shape of the equation\n\nA scalar-valued equation counts as m = 1 here, so batching it with hcat gives a 1timesN matrix.\n\nAll data arguments must have the same number of dimensions and the same batch size.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.AbstractBatchedFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.Derivative","page":"API","text":"Derivative\n\nSupertype of the symbolic derivatives this package computes: Jacobian (with respect to the input of a network) and Gradient (with respect to its parameters). Use derivative to get the symbolic expression out of one.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.Derivative"},{"category":"type","location":"api/#SymbolicNeuralNetworks.EquationSetArrayFunction","page":"API","text":"EquationSetArrayFunction{NDATA}(functions)\n\nThe function build_nn_function returns for an array of EquationSets: it evaluates one EquationSetFunction per entry and collects the results.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.EquationSetArrayFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.EquationSetFunction","page":"API","text":"EquationSetFunction{NDATA}(f, layout)\n\nThe function build_nn_function returns for an EquationSet: it evaluates the jointly generated f and puts the flat result back into the nesting recorded in layout.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.EquationSetFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.FlatParameterFunction","page":"API","text":"FlatParameterFunction{NDATA}(f, layout)\n\nThe function build_flat_function returns: it lays its flat parameter argument out into the shape layout describes and calls f with it.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.FlatParameterFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.Gradient","page":"API","text":"Gradient <: Derivative\n\nComputes and stores the derivative of a symbolic expression with respect to the parameters of a SymbolicNeuralNetwork.\n\nConstructors\n\nGradient(f, nn)\n\nDifferentiate the symbolic f with respect to the parameters of nn.\n\nGradient(nn)\n\nDifferentiate the symbolic output of nn, i.e. nn.model(nn.input, params(nn)).\n\nExamples\n\nusing SymbolicNeuralNetworks: SymbolicNeuralNetwork, Gradient, derivative\nusing AbstractNeuralNetworks\n\nc = Chain(Dense(2, 1, tanh))\nnn = SymbolicNeuralNetwork(c)\n(Gradient(nn) |> derivative)[1].L1.b\n\n# output\n\n1-element Vector{Symbolics.Num}:\n 1 - (tanh(W_2₁ + W_1₁ˏ₁*x₁ + W_1₁ˏ₂*x₂)^2)\n\nImplementation\n\nInternally this uses symbolic_parameter_gradient. For an array-valued f the result is an array of the same shape whose entries are the parameter-shaped gradients of the corresponding entry of f — so the gradient of a matrix is a matrix of NetworkParameters, each of which is the ordinary gradient of one matrix element.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.Gradient"},{"category":"type","location":"api/#SymbolicNeuralNetworks.InPlaceBatchedFunction","page":"API","text":"InPlaceBatchedFunction{NDATA}(kernel!, equation_size, reduction)\n\nAn AbstractBatchedFunction that allocates the result once and lets its kernel write every batch column into it. This is the default of build_nn_function; it costs a single allocation per call rather than one per column, at the price of not being differentiable by Zygote (Mutating arrays is not supported). Forward-mode AD works either way, as the element type of the preallocated array is promoted over the inputs — see promoted_eltype.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.InPlaceBatchedFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.Jacobian","page":"API","text":"Jacobian <: Derivative\n\nComputes and stores the derivative of a symbolic expression with respect to the input of a SymbolicNeuralNetwork.\n\nConstructors\n\nJacobian(f, nn)\nJacobian(nn)\n\nDifferentiate the symbolic f with respect to the input of nn. If f is not supplied it is taken to be the symbolic output of the network, nn.model(nn.input, params(nn)).\n\nFields\n\nf: the symbolic expression that was differentiated,\n□: the symbolic Jacobian,\nnn: the SymbolicNeuralNetwork.\n\nImplementation\n\nFor a function fmathbbR^ntomathbbR^m we use the convention\n\nsquare_ij = fracpartialpartialx_jf_i text ie  square in mathbbR^mtimesn\n\nwhich is also the one Zygote and ForwardDiff use. An f that is not a vector is flattened with vec first, so the rows of □ are indexed by vec(f); a scalar f gives a 1timesn Jacobian, i.e. its gradient with respect to the input as a row.\n\nExamples\n\nHere we compute the Jacobian of a single-layer neural network x mapsto mathrmtanh(Wx + b), whose element-wise derivative is\n\n    fracpartialpartialx_isigmaleft(sum_kw_jkx_k + b_jright) = sigmaleft(sum_kw_jkx_k + b_jright)w_ji\n\nand compare it to that expression. Note that mathrmtanh(x) = frac4e^2x(e^2x + 1)^2\n\nusing SymbolicNeuralNetworks\nusing SymbolicNeuralNetworks: Jacobian, derivative\nusing AbstractNeuralNetworks: Dense, Chain, NeuralNetwork, params\nimport Random\n\nRandom.seed!(123)\n\ninput_dim = 5\noutput_dim = 2\nc = Chain(Dense(input_dim, output_dim, tanh))\nnn = SymbolicNeuralNetwork(c)\njacobian = build_nn_function(derivative(Jacobian(nn)), nn)\n\nps = params(NeuralNetwork(c, Float64))\ninput = rand(input_dim)\nDtanh(x::Real) = 4 * exp(2 * x) / (1 + exp(2x)) ^ 2\nanalytic_jacobian(i, j) = Dtanh(sum(k -> ps.L1.W[j, k] * input[k], 1:input_dim) + ps.L1.b[j]) * ps.L1.W[j, i]\njacobian(input, ps) ≈ [analytic_jacobian(i, j) for j ∈ 1:output_dim, i ∈ 1:input_dim]\n\n# output\n\ntrue\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.Jacobian"},{"category":"type","location":"api/#SymbolicNeuralNetworks.LayerStep","page":"API","text":"LayerStep{Key}(layer, dλ, dθ)\n\nOne step of the adjoint sweep: the layer, the two generated functions the backward pass calls for it, and — as a type parameter — the key its parameters have in the parameter set.\n\nBoth functions take (x, λ, ps): the layer's own input, the sensitivity of the loss to the layer's output, and the parameters of the whole network. dλ returns the sensitivity with respect to the layer's input, dθ the derivative of the loss with respect to the layer's parameters.\n\nA layer that carries data alongside the state takes that data in between, as further arguments before λ; seam_arguments is what produces them, and seam_interface the whole picture.\n\ndλ is nothing for the first step of a sweep, which is the one place it is never called; see layer_step.\n\nTaking the whole parameter set rather than the layer's own entry is what avoids a wrapper per call: the generated kernels were built from symbolic parameters nested under Key, so they reach for ps.<Key>.W and nothing has to be rebuilt for them.\n\nKey is a type parameter rather than a field so that reading the layer's own entry out of the parameter set — step_parameters, which the forward pass needs — is an inferable getproperty on a name the compiler knows, rather than one on a Symbol it does not. Without it a chain whose layers hold differently shaped parameters infers their union.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.LayerStep"},{"category":"type","location":"api/#SymbolicNeuralNetworks.LayerwiseGradientFunction","page":"API","text":"LayerwiseGradientFunction{Keys}(steps, seed)\n\nWhat a layerwise SymbolicPullback stores in place of a single generated gradient function.\n\nApplying it to an input, a target output and the parameters runs the sweep and returns the derivative of the loss with respect to the parameters, as a NetworkParameters — the same thing the monolithic gradient function returns, so everything above it is unchanged.\n\nKeys are the keys of the parameter set, as a type parameter so that the returned NamedTuple is inferable; see LayerStep for the same reasoning one level down.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.LayerwiseGradientFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.OutOfPlaceBatchedFunction","page":"API","text":"OutOfPlaceBatchedFunction{NDATA}(kernel, equation_size, reduction)\n\nAn AbstractBatchedFunction that evaluates its kernel once per batch column and combines the results with Base.reduce. It allocates an array per column, but — unlike InPlaceBatchedFunction — it does not mutate anything and can therefore be differentiated by Zygote.\n\nThis is what build_nn_function returns for inplace = false, and for scalar-valued equations, for which Symbolics.build_function emits no in-place form.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.OutOfPlaceBatchedFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.ParameterGradient","page":"API","text":"ParameterGradient(gradient_function)\n\nWhat a SymbolicPullback stores in its fun field. Applying it to an input, an output and the network parameters returns the PullbackFunction for those.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.ParameterGradient"},{"category":"type","location":"api/#SymbolicNeuralNetworks.PassThroughLayer","page":"API","text":"PassThroughLayer{N}()\n\nA layer that returns its input unchanged, with no parameters. Used to obtain the loss as a function of the network's prediction rather than of its input; see loss_expression.\n\nAbstractNeuralNetworks has no such interface: a NetworkLoss is applied as loss(model, ps, input, output), so the only way to ask it \"what are you, as a function of the prediction and the target?\" is to hand it a model whose prediction is its input.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.PassThroughLayer"},{"category":"type","location":"api/#SymbolicNeuralNetworks.PullbackFunction","page":"API","text":"PullbackFunction(gradient_function, input, output, parameters)\n\nThe function a SymbolicPullback returns as the second entry of its result. It takes the output sensitivities — which it ignores, as the loss is scalar-valued, see the extended help of SymbolicPullback — and returns the derivative of the loss with respect to the network parameters, as a NamedTuple.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.PullbackFunction"},{"category":"type","location":"api/#SymbolicNeuralNetworks.SymbolicNeuralNetwork","page":"API","text":"SymbolicNeuralNetwork <: AbstractSymbolicNeuralNetwork\n\nA symbolic representation of a (small) neural network.\n\nIt pairs a model with symbolic stand-ins for its parameters and its input, so that symbolic expressions can be built from it:\n\nc = Chain(Dense(2, 1, tanh))\nnn = SymbolicNeuralNetwork(c)\neq = c(nn.input, params(nn))\n\nThose expressions can then be differentiated (Jacobian, Gradient, SymbolicPullback) and turned into executable code with build_nn_function.\n\nFields\n\narchitecture: the neural network architecture,\nmodel: the model (typically a Chain that realizes the architecture),\nparams: the symbolic parameters of the network, with the same nesting as the numeric ones,\ninput: the symbolic input of the network, a Vector{Num}.\n\nConstructors\n\nSymbolicNeuralNetwork(nn)\nSymbolicNeuralNetwork(arch, model)\nSymbolicNeuralNetwork(model)\nSymbolicNeuralNetwork(arch)\n\nBuild a SymbolicNeuralNetwork from an AbstractNeuralNetworks.NeuralNetwork, from an architecture and/or a model, or from a single layer.\n\nImplementation\n\nParameters and input are built by symbolic_variables, i.e. they consist of scalar symbolic variables. The parameters are named W_1, W_2, … in the order in which they appear in the parameter set, the input entries x₁, x₂, ….\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.SymbolicNeuralNetwork"},{"category":"type","location":"api/#SymbolicNeuralNetworks.SymbolicPullback","page":"API","text":"SymbolicPullback <: AbstractPullback\n\nThe symbolic pullback of a loss function: it evaluates the loss and the derivative of the loss with respect to the network parameters from generated code instead of by automatic differentiation.\n\nConstructors\n\nSymbolicPullback(nn, loss)\nSymbolicPullback(nn)\n\nBuild the pullback of loss (an AbstractNeuralNetworks.NetworkLoss, by default a FeedForwardLoss) for the SymbolicNeuralNetwork nn.\n\nExamples\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks\nusing AbstractNeuralNetworks: params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 1, tanh))\nnn = NeuralNetwork(c)\nsnn = SymbolicNeuralNetwork(nn)\npb = SymbolicPullback(snn, FeedForwardLoss())\nps = params(nn)\ntypeof(pb(ps, nn.model, (rand(2), rand(1)))[2](1))\n\n# output\n\n@NamedTuple{L1::@NamedTuple{W::Matrix{Float64}, b::Vector{Float64}}}\n\nKeyword Arguments\n\nlayerwise: how the pullback is built (default :auto). With :auto it is composed layer by layer whenever that is the better choice (composes_layerwise), and built from one expression for the whole network otherwise. true demands the layerwise construction and errors if it does not apply, with a message naming the reason; false demands the monolithic one. See layerwise_gradient_function and monolithic_gradient_function — the two produce the same gradient, and differ by orders of magnitude in what it costs to build.\ncse: perform common subexpression elimination when generating code (default true). This matters most on the monolithic path: without it every one of the 2 * n_layers generated blocks re-emits the entire forward pass, which makes the code for networks with more than one hidden layer intractably large. See build_kernel.\ninplace: evaluate a batch with an in-place kernel (default true). The pullback is the end of the differentiation chain, so nothing differentiates through it and the default is what you want here; inplace = false exists for symmetry with build_nn_function.\n\nImplementation\n\nAn instance stores\n\nloss: the NetworkLoss,\nfun: a ParameterGradient that produces the pullback function.\n\nCalling the functor on ps, model and an (input, output) tuple returns\n\npullback.loss(model, ps, input, output), pullback.fun(input, output, ps)\n\nwhere the second entry is again a function — of the output sensitivities.\n\nExtended help\n\ninfo: Reverse Accumulation\nIn machine learning we typically do reverse accumulation to perform automatic differentiation (AD). Assuming we are given a function that is the composition of simpler functions f = f_1circf_2circcdotscircf_nmathbbR^ntomathbbR^m reverse differentiation starts with output sensitivities and then successively feeds them through f_n, f_n-1 etc. So it does:(nabla_xf)^T = (nabla_xf_1)^T(nabla_f_1(x)f_2)^Tcdots(nabla_f_n-1(cdotsx)f_n)^T(do)where doinmathbbR^m are the output sensitivities and the jacobians are stepwise multiplied from the left. So we propagate from the output stepwise back to the input. If we have m=1, i.e. if the output is one-dimensional, then the output sensitivities may simply be taken to be do = 1.\n\nA NetworkLoss is scalar-valued, so the extra step of returning a function of the output sensitivities is not strictly necessary here — the equivalent of pb.fun(input, output, ps)(1) could be stored directly. It is however customary for a pullback to return a callable, which is why this package does so too.\n\nThe pullback is the derivative of a loss summed over the batch, which is why the generated function is built with reduce = +:\n\nusing SymbolicNeuralNetworks\nusing SymbolicNeuralNetworks: symbolic_parameter_gradient\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, FeedForwardLoss, params, output_dimension\nusing Symbolics\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 1, tanh))\nnn = NeuralNetwork(c)\nsnn = SymbolicNeuralNetwork(nn)\nloss = FeedForwardLoss()\ninput_output = (rand(2), rand(1))\n\npb_values = SymbolicPullback(snn, loss)(params(nn), nn.model, input_output)[2](1)\n\nsoutput = Symbolics.variables(:y, 1:output_dimension(nn.model))\ngradient = symbolic_parameter_gradient(loss(nn.model, params(snn), snn.input, soutput), snn)\npb_values2 = build_nn_function(gradient, params(snn), snn.input, soutput; reduce = +)(input_output..., params(nn))\n\npb_values == params(pb_values2)\n\n# output\n\ntrue\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.SymbolicPullback"},{"category":"method","location":"api/#AbstractNeuralNetworks.input_dimension-Tuple{AbstractNeuralNetworks.Chain}","page":"API","text":"input_dimension(c::Chain)\noutput_dimension(c::Chain)\n\nThe dimensions a Chain maps between, taken from its first and last layer.\n\nAbstractNeuralNetworks defines both for an AbstractLayer; these methods extend them to a whole Chain, which is what SymbolicNeuralNetwork needs to know how many symbolic input variables to build. They belong upstream too, see issue #35.\n\n\n\n\n\n","title":"AbstractNeuralNetworks.input_dimension"},{"category":"method","location":"api/#SymbolicNeuralNetworks._assert_distinct_seam_variables-Tuple{Any, Tuple, Any}","page":"API","text":"_assert_distinct_seam_variables(layer, sdata, sλ)\n\nReject a seam whose variables are not pairwise distinct, which is what a carried_variables that reuses the names layer_seed gives the state (x) or the sensitivities (λ) produces.\n\nNothing further down would notice. Symbolics.build_function binds one symbolic array to two argument slots and the generated code reads both of them from the last one, so the kernels are built, they run, and they return a gradient that is simply wrong — the one failure this construction must not have, and the reason checked_guess and represents_loss exist one level up.\n\nImplementation\n\nHere rather than in layer_seed, because checked_layer_seed catches everything layer_seed throws and turns it into a decline. A layer whose carried_variables collides is a bug in that layer, not a chain to quietly fall back on, so the check belongs on the far side of that try — and this is where the variables are handed to build_nn_function, which is where the damage would be done.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks._assert_distinct_seam_variables"},{"category":"method","location":"api/#SymbolicNeuralNetworks._assert_no_reserved_names_in_body-Tuple{Any}","page":"API","text":"_assert_no_reserved_names_in_body(body)\n\nReject a generated body that already contains one of the names the kernels give their own arguments.\n\nA symbolic variable that is passed to Symbolics.build_function becomes an argument and is renamed to ˍ₋argN, but one that is not — a variable left free in the equation, i.e. neither a data variable nor a parameter — survives into the body under its own name. If that name happens to be k, the kernel's batch index binds it and the equation silently evaluates with the column number in place of the variable; if it is ps, it binds the parameter set. Neither is caught by _assert_no_name_clash, which only sees the argument names.\n\nThe check has to run before the arguments are substituted, since afterwards the reserved names are all over the body legitimately. At that point the only symbols in the tree are ˍ₋argN, var\"##cse#N\" and literals — functions are embedded as objects — so a reserved name can only have come from a free variable.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks._assert_no_reserved_names_in_body"},{"category":"method","location":"api/#SymbolicNeuralNetworks._float_if_integer-Union{Tuple{Type{T}}, Tuple{T}} where T<:Integer","page":"API","text":"_float_if_integer(T)\n\nfloat(T) for integer types, T for everything else. See allocate_batch_output. Bool is included, as float(Bool) is Float64.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks._float_if_integer"},{"category":"method","location":"api/#SymbolicNeuralNetworks._rewrite_body-Tuple{Expr, Tuple, Tuple, Union{Nothing, Symbol}}","page":"API","text":"_rewrite_body(expression, data_names, parameter_paths, output_name)\n\nApply the rewrite rules of src/codegen/expression_rewriting.jl to the body of a generated function, in the order they depend on each other: the arguments are renamed first so that the later rules can recognise the data arguments, the array constructor is fixed before the batch index is added so that the typeof(…) it contains is not mistaken for a use of a data argument.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks._rewrite_body"},{"category":"method","location":"api/#SymbolicNeuralNetworks.access_expression-Tuple{Symbol, Tuple}","page":"API","text":"access_expression(name, path)\n\nThe expression that reads path out of the object called name, e.g. ps.L1.W for (:L1, :W).\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.access_expression"},{"category":"method","location":"api/#SymbolicNeuralNetworks.accumulate_into_output-Tuple{Any, Symbol, Any, Integer}","page":"API","text":"accumulate_into_output(expr, output_name, reduction, equation_length)\n\nRewrite the out[i] = … assignments of in-place generated code so that a single preallocated array can hold the result of a whole batch.\n\nWhich rewrite applies depends on how the per-sample results are combined:\n\nreduction = +: the writes become +=, so every sample accumulates into the same buffer (which allocate_batch_output zeroes).\nreduction = hcat: the writes are shifted by (k - 1)cdotmathrmequation_length, which is the offset of block k in the column-major layout of the concatenated result.\n\nThe generated code addresses its output with a single linear index whatever the shape of the equation, which is what makes that offset arithmetic correct. equation_length assignments are expected, one per entry of the equation; anything else throws, because a replace that matches nothing would leave a kernel that still compiles and runs but writes every sample to the same place.\n\nExamples\n\nusing SymbolicNeuralNetworks: accumulate_into_output\n\naccumulate_into_output(:(out[1] = a), :out, hcat, 1)\n\n# output\n\n:(out[1 + (k - 1) * 1] = a)\n\nusing SymbolicNeuralNetworks: accumulate_into_output\n\naccumulate_into_output(:(out[1] = a), :out, +, 1)\n\n# output\n\n:(out[1] += a)\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.accumulate_into_output"},{"category":"method","location":"api/#SymbolicNeuralNetworks.adjoint_step-Tuple{Tuple, Vararg{Any, 4}}","page":"API","text":"adjoint_step(steps, x, ps, seed, output)\n\nThe sensitivity of the loss to x and the parameter gradients of steps, given that x is what the remaining steps are applied to. See sweep.\n\nThe recursion bottoms out at the network's output, which the seed takes together with the target. So the chain's last layer has to return the model's output and nothing beside it — a layer that carries data through cannot be the last one, which is also what the monolithic construction and the loss itself require.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.adjoint_step"},{"category":"method","location":"api/#SymbolicNeuralNetworks.allocate_batch_output-Union{Tuple{T}, Tuple{Type{T}, Tuple{}, Integer, typeof(+)}} where T","page":"API","text":"allocate_batch_output(T, equation_size, batch_size, reduction)\n\nAllocate the result of evaluating an equation of size equation_size over batch_size samples, with the shape documented for AbstractBatchedFunction.\n\nT is widened with float when it is an integer type: it comes from promoted_eltype, i.e. from the inputs, and an equation over integer inputs generally does not evaluate to an integer.\n\nA scalar-valued equation — equation_size == () — counts as one of size m = 1, so hcat gives a 1timesN matrix and + a number. Those two methods are only reached for an empty batch: the in-place path, which is what allocates a result up front, does not exist for a scalar equation.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.allocate_batch_output"},{"category":"method","location":"api/#SymbolicNeuralNetworks.allocate_single_output-Union{Tuple{T}, Tuple{Type{T}, Tuple, typeof(+)}} where T","page":"API","text":"allocate_single_output(T, equation_size, reduction)\n\nAllocate the result of evaluating an equation of size equation_size for a single sample. Unlike allocate_batch_output this keeps the shape of the equation, as the result may itself be a matrix.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.allocate_single_output"},{"category":"method","location":"api/#SymbolicNeuralNetworks.argument_substitutions-Tuple{AbstractVector, Tuple, Tuple}","page":"API","text":"argument_substitutions(generated_names, data_names, parameter_paths; output_name)\n\nMap the argument names of the generated function onto the names the kernel uses, by position.\n\nSymbolics.build_function was handed the data variables first and then one argument per parameter array (see parameter_arguments), so with output_name set — the in-place form prepends an output argument — the correspondence is\n\n(ˍ₋out, ˍ₋arg1, …, ˍ₋arg_ndata, ˍ₋arg_{ndata+1}, …)  ↦  (out, x1, …, x_ndata, ps.L1.W, …)\n\nEvery parameter argument becomes a chain of getproperty calls on the single parameter argument ps, which is what lets the kernel be called with NetworkParameters instead of a flat argument list.\n\nExamples\n\nusing SymbolicNeuralNetworks: argument_substitutions\n\nsubstitutions = argument_substitutions([:ˍ₋arg1, :ˍ₋arg2], (:x1,), (((:L1, :W)),); output_name = nothing)\n(substitutions[:ˍ₋arg1], substitutions[:ˍ₋arg2])\n\n# output\n\n(:x1, :(ps.L1.W))\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.argument_substitutions"},{"category":"method","location":"api/#SymbolicNeuralNetworks.batched-Tuple{AbstractVector}","page":"API","text":"batched(data)\n\ndata with any batch dimensions past the first collapsed into it, which is the shape the sweep evaluates in. For an input that carries data alongside the state, only the state is laid out; the rest is seam_arguments' business.\n\nImplementation\n\nbuild_nn_function accepts a result of mtimesN_1timesN_2 as a batch with two batch dimensions (see AbstractBatchedFunction), and the monolithic construction of SymbolicPullback therefore evaluates one. The layerwise sweep cannot pass such an array to a layer — the forward pass is the layer called, and a Dense multiplies a matrix by a matrix — so the batch is laid out flat first.\n\nThat loses nothing. The pullback of a batch is the sum of the per-sample gradients, so how the samples are arranged cannot change it; the two shapes are checked against each other in test/derivatives/layerwise_pullback.jl. Nothing has to be restored afterwards either, for the same reason: what comes back is shaped like the parameters, not like the batch.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.batched"},{"category":"method","location":"api/#SymbolicNeuralNetworks.build_flat_function-Tuple{Any, AbstractSymbolicNeuralNetwork}","page":"API","text":"build_flat_function(eq, nn)\nbuild_flat_function(eq, nn, soutput)\nbuild_flat_function(eq, sparams, svariables...)\n\nTurn a symbolic equation into an executable function whose parameter argument is a flat vector:\n\nbuilt_function(input, w)\nbuilt_function(input, output, w)\n\nw may be a plain AbstractVector — in which case it is read through the layout of the parameters the equation was built from — or a NeuralNetworkParameters.FlatParameters, which carries its own.\n\nTakes the same keyword arguments as build_nn_function, and accepts everything it accepts, including an EquationSet.\n\nThe third form — the symbolic parameters and the symbolic data variables given directly rather than taken from a network — is the one for degrees of freedom that are not a network's parameters: nothing here reads a model. It is what flat_parameter_gradient's second form pairs with. Unlike that one it needs a NetworkParameters rather than any EquationSet, because build_nn_function dispatches on one.\n\nExamples\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nusing NeuralNetworkParameters: flatten\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 1, tanh))\nnn = NeuralNetwork(c)\nsnn = SymbolicNeuralNetwork(nn)\nf = build_flat_function(c(snn.input, params(snn)), snn)\n\nw, _ = flatten(params(nn))\nf([1.0, 2.0], w) ≈ c([1.0, 2.0], params(nn))\n\n# output\n\ntrue\n\nImplementation\n\nThe flat vector is unflattened and the ordinary generated function called on the result, rather than the code generation being changed to index into the vector. The conversion is a copyto! per leaf and costs a fraction of a forward pass; generating a different kernel would save that and lose the ability to hand the same equation a structured parameter set.\n\nThe conversion is unflatten and not unflatten! — it allocates the parameter set rather than writing into the one the layout was built from, so w may have a different element type, which is what lets ForwardDiff differentiate with respect to the flat form. This is unrelated to the inplace keyword, which says how the generated kernel evaluates a batch and is passed through untouched.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.build_flat_function"},{"category":"method","location":"api/#SymbolicNeuralNetworks.build_kernel!-Tuple{Any, NeuralNetworkParameters.NetworkParameters, Vararg{Any}}","page":"API","text":"build_kernel!(equation, sparams, svariables...; reduction, cse)\n\nBuild an in-place kernel that writes the result for batch column k into a preallocated array:\n\nkernel!(out, x1, ps, k)          # one data argument\nkernel!(out, x1, x2, ps, k)      # two data arguments\n\nWhere in out the result goes depends on reduction; see accumulate_into_output.\n\nReturns nothing for a scalar-valued equation, for which Symbolics.build_function emits no in-place form.\n\nEvaluating a batch with such a kernel costs a single allocation instead of one array per column plus a Base.reduce fold, but the result is produced by mutation and can therefore not be differentiated by Zygote. See build_kernel for the keyword arguments.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.build_kernel!"},{"category":"method","location":"api/#SymbolicNeuralNetworks.build_kernel-Tuple{Any, NeuralNetworkParameters.NetworkParameters, Vararg{Any}}","page":"API","text":"build_kernel(equation, sparams, svariables...; cse)\n\nBuild an out-of-place kernel that evaluates equation for batch column k:\n\nkernel(x1, ps, k)          # one data argument\nkernel(x1, x2, ps, k)      # two data arguments\n\nThere is no bound on the number of data arguments; see data_name.\n\nsparams are the symbolic parameters and svariables the symbolic data variables the equation was built from. See build_kernel! for the in-place counterpart and build_nn_function for the function that batching, allocation and reshaping are added to.\n\nExamples\n\nusing SymbolicNeuralNetworks: build_kernel, SymbolicNeuralNetwork\nusing AbstractNeuralNetworks: params, Chain, Dense, NeuralNetwork\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 1, tanh))\nnn = NeuralNetwork(c)\nsnn = SymbolicNeuralNetwork(nn)\nkernel = build_kernel(c(snn.input, params(snn)), params(snn), snn.input)\nkernel([1.0 2.0; 3.0 4.0], params(nn), 1)\n\n# output\n\n1-element Vector{Float64}:\n 0.9912108161055604\n\nKeyword Arguments\n\ncse: perform common subexpression elimination when generating code (default true).\n\nSymbolics stores an expression as a hash-consed directed acyclic graph but Symbolics.build_function prints it as a tree. Every time a subexpression is reused — the output of layer n feeding each neuron of layer n+1, or the forward pass shared by every block of a symbolic gradient — the whole subtree is emitted again, so both the size of the generated code and the amount of redundant arithmetic grow exponentially with the depth of the network. With cse = true the graph is emitted as a let block of intermediate bindings instead, which keeps the code size proportional to the number of distinct nodes.\n\nPass cse = false to recover the fully inlined output; that is mostly useful for debugging, and for very small networks where the binding overhead is not amortised.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.build_kernel"},{"category":"method","location":"api/#SymbolicNeuralNetworks.build_nn_function-Tuple{AbstractArray{<:Union{NamedTuple, NeuralNetworkParameters.NetworkParameters}}, NeuralNetworkParameters.NetworkParameters, Vararg{Union{AbstractVector{Symbolics.Num}, Symbolics.Arr}}}","page":"API","text":"build_nn_function(eqs::AbstractArray{<:EquationSet}, sparams, svariables...)\n\nTurn an array of equation sets into an executable function that returns an array of results.\n\nEach entry of the array is built by the EquationSet method above, i.e. jointly; the entries themselves are independent of each other and stay separate functions.\n\nExamples\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 1, tanh))\nnn = NeuralNetwork(c)\nsnn = SymbolicNeuralNetwork(nn)\neqs = [(a = c(snn.input, params(snn)),), (b = c(snn.input, params(snn)) .^ 3,)]\nfuncs = build_nn_function(eqs, params(snn), snn.input)\nfuncs([1.0, 2.0], params(nn))\n\n# output\n\n2-element Vector{NamedTuple{names, Tuple{Vector{Float64}}} where names}:\n (a = [0.985678060655224],)\n (b = [0.9576465981186686],)\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.build_nn_function"},{"category":"method","location":"api/#SymbolicNeuralNetworks.build_nn_function-Tuple{Any, AbstractSymbolicNeuralNetwork}","page":"API","text":"build_nn_function(eq, nn)\nbuild_nn_function(eq, nn, soutput)\n\nTurn a symbolic equation into an executable function.\n\nThe result is called with the network input and the network parameters, and with the target output in between if the equation was built with one:\n\nbuilt_function(input, ps)\nbuilt_function(input, output, ps)\n\ninput may be a single sample (a vector), a batch (a matrix whose columns are the samples), or a batch with two batch dimensions (a three-dimensional array). See AbstractBatchedFunction for the shape of the result in each case.\n\nbuild_nn_function(eq, sparams, svariables...)\n\nThe same, but with the symbolic parameters and the symbolic data variables given explicitly rather than taken from a SymbolicNeuralNetwork.\n\nKeyword Arguments\n\ncse: perform common subexpression elimination when generating code (default true). See build_kernel.\ninplace: evaluate a batch with an in-place kernel (default true). See below.\nreduce: how to combine the results of the individual samples of a batch, either hcat (default) or +.\n\nwarning: The default result cannot be differentiated with `Zygote`\nWith inplace = true the returned function allocates its result and lets the generated kernel mutate it, which Zygote does not support (Mutating arrays is not supported). Pass inplace = false to get the out-of-place version, which is differentiable but allocates an array per sample. Forward-mode AD (ForwardDiff) works with either. See InPlaceBatchedFunction and OutOfPlaceBatchedFunction.\n\nExamples\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 1, tanh))\nnn = NeuralNetwork(c)\nsnn = SymbolicNeuralNetwork(nn)\nbuilt_function = build_nn_function(c(snn.input, params(snn)), snn)\nbuilt_function([1.0, 2.0], params(nn)) ≈ c([1.0, 2.0], params(nn))\n\n# output\n\ntrue\n\nImplementation\n\nThe equation is scalarised, a kernel that evaluates a single sample is generated from it (build_kernel or build_kernel!), and that kernel is wrapped in an AbstractBatchedFunction which adds the batching. Symbolics.build_function emits no in-place form for a scalar-valued equation, so those always take the out-of-place path.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.build_nn_function"},{"category":"method","location":"api/#SymbolicNeuralNetworks.build_nn_function-Tuple{Union{NamedTuple, NeuralNetworkParameters.NetworkParameters}, NeuralNetworkParameters.NetworkParameters, Vararg{Union{AbstractVector{Symbolics.Num}, Symbolics.Arr}}}","page":"API","text":"build_nn_function(eqs::EquationSet, sparams, svariables...)\n\nTurn a whole set of equations into one executable function, whose result has the same nesting as eqs.\n\nExamples\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 1, tanh))\nnn = NeuralNetwork(c)\nsnn = SymbolicNeuralNetwork(nn)\neqs = (a = c(snn.input, params(snn)), b = c(snn.input, params(snn)) .^ 2)\nfuncs = build_nn_function(eqs, params(snn), snn.input)\nfuncs([1.0, 2.0], params(nn))\n\n# output\n\n(a = [0.985678060655224], b = [0.9715612392570434])\n\nImplementation\n\nAll entries are generated as a single function whose flat result is split up again afterwards; see flatten_equations and split_result. Generating one function per entry instead would re-derive everything the entries have in common — for a symbolic gradient that is the whole forward pass, once per parameter array — and would compile one RuntimeGeneratedFunction per entry rather than one in total.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.build_nn_function"},{"category":"method","location":"api/#SymbolicNeuralNetworks.callee_name-Tuple{Any}","page":"API","text":"callee_name(e)\n\nThe name of the function a call expression calls, or nothing if e is not a call.\n\nThe emitted code refers to a function either by symbol, by function object, or by a qualified path (SymbolicUtils.Code.create_array), depending on how it was constructed. All three forms are reduced to a plain Symbol here so that the rules only have to deal with one of them.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.callee_name"},{"category":"method","location":"api/#SymbolicNeuralNetworks.carried_variables-Tuple{AbstractNeuralNetworks.AbstractExplicitLayer}","page":"API","text":"carried_variables(layer)\n\nFresh symbolic arrays standing for whatever layer carries alongside the state at the seam, as a tuple. () by default, which is the plain-vector seam.\n\nExtending\n\nSymbolicNeuralNetworks.carried_variables(layer::MyLayer) = (Symbolics.variables(:c, 1:length(layer)),)\n\nThe arrays are built here rather than passed in because their shape is the layer's own knowledge — this package knows only input_dimension and output_dimension, which describe the state. Their name is the layer's own to choose too, and has to differ from the ones layer_seed uses itself — x for the state and λ for the sensitivities. Reusing one of those does not make a second array, it names the same one twice; layer_step refuses the seam rather than generating kernels that would quietly read the state where the carried datum should be.\n\nReturn () when there is nothing to carry, rather than an empty array: there would be nothing to hand the generated kernels, and an empty array is not a usable data argument in any case. The seam is then the plain vector it is for every other layer. SymplecticEuler over a system with no parameters is exactly this case.\n\nThe layer may still have to be given something — its functor takes the pair either way — in which case seam_value supplies it as a constant. Nothing varies, so nothing needs a variable, and the constant is folded into the expression like any other. See seam_interface.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.carried_variables"},{"category":"method","location":"api/#SymbolicNeuralNetworks.checked_guess-Tuple{AbstractNeuralNetworks.NetworkLoss, AbstractSymbolicNeuralNetwork, Any, Any}","page":"API","text":"checked_guess(loss, nn, ŷ, y; cse, inplace)\n\nThe guessed expression of loss as a function of prediction and target, or nothing if the guess cannot be trusted.\n\nThere are two ways for it not to be, and both mean the same thing to the caller — decline, and let SymbolicPullback fall back to the monolithic construction:\n\nthe guess disagrees with loss, which is what represents_loss tests for;\nthe guess cannot be built at all. A NetworkLoss need not accept a PassThroughLayer: the generic four-argument method of AbstractNeuralNetworks invites a loss to be written for the model it belongs to, and one written as (::MyLoss)(model::Chain, …) throws when passthrough_expression applies it to a model that is not a Chain. So does a model whose forward pass cannot be evaluated at the points represents_loss checks at.\n\nThe second case has to be caught here rather than left to the caller: layerwise = :auto promises a fallback, and a construction that throws instead of declining would break networks the monolithic path builds perfectly well.\n\nImplementation\n\nThe try covers building and checking the guess, and nothing else. Once an expression is in hand and has been believed, differentiating it and generating code from it are this package's own work, and a failure there is a bug to surface rather than a reason to fall back.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.checked_guess"},{"category":"method","location":"api/#SymbolicNeuralNetworks.checked_layer_seed-Tuple{AbstractNeuralNetworks.AbstractExplicitLayer, Symbol, Any}","page":"API","text":"checked_layer_seed(layer, key, prototype)\n\nWhat layer_seed returns for layer, or nothing when the layer cannot be seeded at all.\n\nA layer that carries something alongside the state and has not declared the seam interface (see seam_interface) has no seed, because the seam it is offered is a plain vector of symbolic variables. There are two ways for that to show, and both mean the same thing to the caller — decline, and let SymbolicPullback fall back to the monolithic construction:\n\nthe layer returns more than the state, so state_expressions' default has no method for its output. This is GeometricMachineLearning's SymplecticEuler with return_parameters = true, which threads the parameters of the system on to the next layer and returns a Tuple;\nthe layer cannot be applied to the bare vector at the seam in the first place, which is what the layer downstream of such a one does — its input is the tuple, and it has no method for anything else.\n\nThe second is why this catches rather than asking applicable(scalar_expressions, layer(sx, ps)): that predicate needs the layer to have been applied already, so it only covers the first.\n\nImplementation\n\nThe try covers building the seed and nothing else — the same line checked_guess draws one level up. Once a seed is in hand, differentiating it and generating code from it are this package's own work, and a failure there is a bug to surface rather than a reason to fall back.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.checked_layer_seed"},{"category":"method","location":"api/#SymbolicNeuralNetworks.composes_layerwise-Tuple{AbstractSymbolicNeuralNetwork}","page":"API","text":"composes_layerwise(nn)\n\nWhether composing the pullback layer by layer is the better choice for nn, which is what layerwise = :auto asks. True when the model decomposes into more than one step.\n\nThis is a question about which construction is preferable, not about whether the layerwise one applies: the layers still have to be seedable (checked_layer_seed) and the loss still has to reduce to a seed (loss_seed), both of which are settled afterwards by layerwise_gradient_function. So a true here can still be followed by a decline.\n\nImplementation\n\nA single layer is the one case where the monolithic construction is unambiguously right: there is no composition to keep out of the expression, so the two build the same derivative, and the seeded form of layer_step merely adds the sensitivity variables and a second generated function.\n\nAbove one layer the two are measured against each other by scripts/codegen_comparison.jl. In expression nodes — the size of the symbolic material each construction has to hold — and in seconds to build, for Dense chains with a FeedForwardLoss:\n\nlayers width parameters monolithic nodes layerwise nodes monolithic layerwise\n2 4 22 6 652 792 0.02 s 0.20 s\n3 4 42 57 772 1 656 0.04 s 0.23 s\n4 4 62 388 700 2 520 0.10 s 0.21 s\n5 4 82 2 317 964 3 384 0.34 s 0.30 s\n6 4 102 12 848 828 4 248 0.65 s 0.32 s\n4 8 186 8 253 148 11 736 0.61 s 0.30 s\n4 16 626 209 455 964 68 760 does not build 0.53 s\n\nThe layerwise node count is exactly linear — 864 more per identical added layer — against a column that multiplies by about six each time.\n\nBuild time crosses over at five layers, or at four of width eight. Below that the monolithic path is still ahead, by at most a fifth of a second, and this returns true there anyway: a threshold that reproduced the crossover would have to be a function of width as well as depth, fitted to timings from one machine, and it would be the wrong thing to get wrong. Choosing layerwise where monolithic would have been quicker costs a fixed fraction of a second; choosing monolithic on a network one layer deeper costs everything.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.composes_layerwise"},{"category":"method","location":"api/#SymbolicNeuralNetworks.data_name-Tuple{Integer}","page":"API","text":"data_name(i)\n\nThe name the generated kernels give their i-th data argument: x1, x2, … There is no bound on how many there may be. One is the network input; a second is typically the target output of a loss; the layerwise pullback uses one per entry of a layer's seam plus one for the output sensitivities (see seam_interface).\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.data_name"},{"category":"method","location":"api/#SymbolicNeuralNetworks.decline-Tuple{Bool, AbstractString}","page":"API","text":"decline(demanded, why)\n\nRefuse the layerwise construction, why saying what stood in the way.\n\nReturns nothing — the signal layerwise = :auto falls back to the monolithic construction on — or throws an ArgumentError naming why, when the caller asked for the construction by name with layerwise = true.\n\nImplementation\n\nBoth outcomes go through here so that layerwise_gradient_function has one decision path: the message is raised where the decline happens rather than reconstructed afterwards by a second traversal of the same checks, which could not help but drift from them.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.decline"},{"category":"method","location":"api/#SymbolicNeuralNetworks.derivative-Tuple{DT} where DT<:SymbolicNeuralNetworks.Derivative","page":"API","text":"derivative(d)\n\nThe symbolic derivative stored in d.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.derivative"},{"category":"method","location":"api/#SymbolicNeuralNetworks.derivative-Tuple{SymbolicNeuralNetworks.Gradient}","page":"API","text":"derivative(g)\n\nThe symbolic gradient stored in g.\n\nExamples\n\nusing SymbolicNeuralNetworks: SymbolicNeuralNetwork, Gradient, derivative, symbolic_parameter_gradient\nusing AbstractNeuralNetworks\n\nc = Chain(Dense(2, 1, tanh))\nnn = SymbolicNeuralNetwork(c)\ng = Gradient(nn)\n\nisequal(derivative(g), symbolic_parameter_gradient(g.f, nn))\n\n# output\n\ntrue\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.derivative"},{"category":"method","location":"api/#SymbolicNeuralNetworks.derivative-Tuple{SymbolicNeuralNetworks.Jacobian}","page":"API","text":"derivative(j)\n\nThe symbolic Jacobian stored in j.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.derivative"},{"category":"function","location":"api/#SymbolicNeuralNetworks.evaluate_batch","page":"API","text":"evaluate_batch(f, data, ps)\n\nEvaluate an AbstractBatchedFunction over the batch held in the tuple of data arguments data. There is one method per rank of the data arguments; the matrix one is the workhorse and the other two reduce to it.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.evaluate_batch"},{"category":"method","location":"api/#SymbolicNeuralNetworks.flat_parameter_gradient-Tuple{Any, Union{NamedTuple, NeuralNetworkParameters.NetworkParameters, AbstractSymbolicNeuralNetwork}}","page":"API","text":"flat_parameter_gradient(f, nn)\nflat_parameter_gradient(f, sparams)\n\nDifferentiate the symbolic expression f with respect to the parameters of nn — or with respect to a set of symbolic parameters sparams given directly — as one flat object rather than as a set nested like the parameters.\n\nA scalar f gives a vector of length flatlength(params(nn)); an array-valued f gives the mathrmlength(f)timesmathrmflatlength Jacobian\n\nJ_ij = fracpartialf_ipartialw_j\n\nwith the rows indexed by vec(f) — the convention Jacobian uses for the derivative with respect to the input, and the matrix a Newton step is built from.\n\nPair it with build_flat_function for a function that is flat in both directions, and with NeuralNetworkParameters.unflatten to read a column block of the result back as the entry of the parameter set it belongs to.\n\nThe sparams form is the one for degrees of freedom that are not a network's parameters: nothing in either function reads a model, so an expression over any NetworkParameters of symbolic leaves goes through both. Here sparams may be any EquationSet, as with symbolic_parameter_gradient; build_flat_function is the narrower of the two and wants a NetworkParameters.\n\nExamples\n\nThe gradient of a scalar expression, against the same derivative in its nested form:\n\nusing SymbolicNeuralNetworks\nusing SymbolicNeuralNetworks: symbolic_parameter_gradient\nusing AbstractNeuralNetworks: Chain, Dense, params\nusing NeuralNetworkParameters: flatten\n\nc = Chain(Dense(2, 1, tanh))\nsnn = SymbolicNeuralNetwork(c)\nscalar = sum(c(snn.input, params(snn)))\n\nflat = flat_parameter_gradient(scalar, snn)\nnested, _ = flatten(symbolic_parameter_gradient(scalar, snn))\n(length(flat), all(isequal.(flat, nested)))\n\n# output\n\n(3, true)\n\nThe Jacobian of a vector-valued one:\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, params\n\nc = Chain(Dense(2, 3, tanh))\nsnn = SymbolicNeuralNetwork(c)\nsize(flat_parameter_gradient(c(snn.input, params(snn)), snn))\n\n# output\n\n(3, 9)\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.flat_parameter_gradient"},{"category":"method","location":"api/#SymbolicNeuralNetworks.flatten_equations-Tuple{Union{NamedTuple, NeuralNetworkParameters.NetworkParameters}}","page":"API","text":"flatten_equations(eqs)\n\nConcatenate every entry of eqs into one vector of scalar equations, together with the NeuralNetworkParameters.ParameterLayout that records where each entry went.\n\nExamples\n\nusing SymbolicNeuralNetworks: flatten_equations, SymbolicNeuralNetwork\nusing AbstractNeuralNetworks: Chain, Dense, params\nusing NeuralNetworkParameters: parameterrange\n\nc = Chain(Dense(2, 3, tanh))\nsnn = SymbolicNeuralNetwork(c)\nflat, layout = flatten_equations((a = c(snn.input, params(snn)), b = c(snn.input, params(snn)) .^ 2))\n(length(flat), parameterrange(layout.children.a), parameterrange(layout.children.b))\n\n# output\n\n(6, 1:3, 4:6)\n\nImplementation\n\nThe layout is the one NeuralNetworkParameters builds for a parameter set: an equation set has the same shape as one, its leaves are arrays (or single instances) of Num, and the layout records exactly what splitting the flat result needs — a range and a size per leaf, in the order the leaves are written. See unflatten_batch for the one thing that has to be added on top, and NeuralNetworkParameters.unflatten for the vector case, which needs nothing.\n\nEach entry is normalised by scalar_expressions on the way in, which is what turns a Symbolics.Arr leaf into the Array{Num} the layout expects. The element type of the flat vector is fixed to Num rather than left to NeuralNetworkParameters.parameter_eltype to promote, so that it is the same type for every equation set — the code generation downstream dispatches on it.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.flatten_equations"},{"category":"method","location":"api/#SymbolicNeuralNetworks.flatten_gradient-Tuple{Union{NamedTuple, NeuralNetworkParameters.NetworkParameters}}","page":"API","text":"flatten_gradient(gradient)\n\nLay a symbolic parameter derivative out flat: a single parameter-shaped set becomes a vector, and an array of them — which is what differentiating an array-valued expression gives, one set per entry — becomes a matrix with one row per entry. See flat_parameter_gradient.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.flatten_gradient"},{"category":"method","location":"api/#SymbolicNeuralNetworks.function_arguments_and_body-Tuple{Expr}","page":"API","text":"function_arguments_and_body(expr)\n\nSplit a generated function expression into its vector of argument names and its body.\n\nThrows an ArgumentError if expr is not of the shape Symbolics.build_function is documented to return, which turns an upstream change into an error at code-generation time rather than into subtly wrong code.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.function_arguments_and_body"},{"category":"method","location":"api/#SymbolicNeuralNetworks.function_expression-Tuple{Any, Any}","page":"API","text":"function_expression(argument_names, body)\n\nAssemble an anonymous function definition; the inverse of function_arguments_and_body.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.function_expression"},{"category":"method","location":"api/#SymbolicNeuralNetworks.generated_expression-Tuple{Any, Tuple, Tuple}","page":"API","text":"generated_expression(equation, svariables, sarrays; inplace, cse)\n\nCall Symbolics.build_function and pick the half of its output that is asked for.\n\nIt returns an (out_of_place, in_place) pair for an array-valued equation and a single expression for a scalar-valued one; nothing is returned when the in-place half is asked for and there is none.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.generated_expression"},{"category":"method","location":"api/#SymbolicNeuralNetworks.index_by_batch-Tuple{Any, Tuple}","page":"API","text":"index_by_batch(expr, data_names)\n\nTurn x[i] into x[i, k] for every data argument x.\n\nThe generated code reads a data argument as if it were a single sample. Adding the batch index k as a second index is what makes the same code read column k of a matrix instead, and is the reason the kernels take a batch index at all.\n\nBoth forms Symbolics emits for reading an entry are handled: x[i] (Expr(:ref, …)) and getindex(x, i), which it uses for arguments that were Symbolics.Arrs.\n\nThrows an ArgumentError if a data argument is used other than by indexing it with a single index, because then the batch dimension would silently be ignored for that use.\n\nExamples\n\nusing SymbolicNeuralNetworks: index_by_batch\n\nindex_by_batch(:(x1[1] + getindex(x1, 2)), (:x1,))\n\n# output\n\n:(x1[1, k] + getindex(x1, 2, k))\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.index_by_batch"},{"category":"method","location":"api/#SymbolicNeuralNetworks.is_reserved_name-Tuple{Symbol}","page":"API","text":"is_reserved_name(name)\n\nWhether name is one a generated kernel gives an argument of its own, and therefore one a symbolic variable left free in an equation may not carry.\n\nThat is FIXED_NAMES together with the whole x1, x2, … family of data_names — the family and not just the arities in use, because whether a given name is generated would otherwise depend on how many data arguments the equation happens to have, and a free variable named x3 would pass the check today and break the day a third data argument arrived.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.is_reserved_name"},{"category":"method","location":"api/#SymbolicNeuralNetworks.layer_seed-Tuple{AbstractNeuralNetworks.AbstractExplicitLayer, Symbol, Any}","page":"API","text":"layer_seed(layer, key, prototype)\n\nThe scalar one layer's two derivatives are taken of, together with the variables it is written in: (seed, sparams, sdata, sλ), where\n\nmathrmseed = lambda_k cdot f_k(x_k-1 theta_k)\n\nsparams nests the layer's parameters under key, with the shape of prototype, so that the code generated from seed reaches into the parameter set of the whole network — see LayerStep. sdata is the tuple of data variables the seam is written in — the state first, then whatever carried_variables declared — and it and sλ are fresh for every layer, which is what keeps an expression built from this one dependent on that layer alone.\n\nA layer that carries nothing gets sdata = (sx,), one plain vector, and this is then the construction it always was. A layer that carries something and has not declared the seam interface cannot be seeded at all — it either returns more than state_expressions' default can take apart, or has no method for a bare vector in the first place; checked_layer_seed is what turns that into a decline rather than an exception. See seam_interface.\n\nSeparate from layer_step so that scripts/codegen_comparison.jl measures the symbolic material this construction actually holds, rather than a second copy of it that can drift.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.layer_seed"},{"category":"method","location":"api/#SymbolicNeuralNetworks.layer_step-Tuple{AbstractNeuralNetworks.AbstractExplicitLayer, Symbol, Tuple}","page":"API","text":"layer_step(layer, key, seeded; input_sensitivity, cse, inplace)\n\nBuild the LayerStep of one layer from seeded, the tuple layer_seed returns.\n\nThe seed is passed in rather than built here so that every layer of a chain can be seeded — and the chain declined if one of them cannot be, see checked_layer_seed — before any code is generated for any of them. Building a seed is one symbolic forward pass through one layer; generating its two kernels is the expensive half, and a chain that will be declined should not pay it.\n\nA seam whose variables are not pairwise distinct is refused here rather than declined; see _assert_distinct_seam_variables.\n\ninput_sensitivity = false leaves out the derivative with respect to the layer's input, for the first layer of a chain, whose sensitivity is that of the loss to the network's input — something a parameter gradient has no use for, and which sweep accordingly never asks for. Generating it anyway would be half the code this function emits, spent on a function that is never called.\n\nImplementation\n\nBoth derivatives come from differentiating the scalar\n\ns_k = lambda_k cdot f_k(x_k-1 theta_k)\n\nin which lambda_k is a fresh vector of symbolic variables standing for the sensitivity of the loss to this layer's output. Its two gradients are precisely what the sweep needs: partials_kpartialx_k-1 is lambda_k-1 and partials_kpartialtheta_k is partialLpartialtheta_k.\n\nSeeding the derivative like this rather than building partialx_kpartialx_k-1 and partialx_kpartialtheta_k separately is what keeps both the expression and the generated code small: neither the Jacobian nor the rank-3 parameter derivative is ever materialised, and no contraction is left to do at run time. For four layers of width 16 the seeded form holds 68 760 nodes against 76 372 for the Jacobian-and-parameter-derivative pair.\n\nThe variables at the seam are fresh for every layer, which is the whole point — an expression built here refers to this layer's input and to nothing upstream of it, so its size depends on this layer alone. That the same names recur across layers is harmless, as each layer is compiled into its own kernel.\n\nThe two derivatives are compiled separately because they are reduced over a batch differently: a sensitivity is per-sample and concatenates, whereas the gradient of a batch is the sum of the per-sample gradients — which is what SymbolicPullback means by the pullback of a batch. So the sweep costs two calls per layer whatever the batch size, with no per-sample loop.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.layer_step"},{"category":"method","location":"api/#SymbolicNeuralNetworks.layerwise_gradient_function-Tuple{SymbolicNeuralNetwork, AbstractNeuralNetworks.NetworkLoss}","page":"API","text":"layerwise_gradient_function(nn, loss; demanded, cse, inplace)\n\nBuild the LayerwiseGradientFunction of loss for nn, or decline when the layerwise construction does not apply. There are three ways for it not to:\n\nthe model does not decompose into steps (symbolic_steps);\none of those steps cannot be seeded (checked_layer_seed);\nthe loss cannot be reduced to a seed (loss_seed).\n\ndemanded = true — which is SymbolicPullback's layerwise = true — makes each of those an error naming the reason instead of the nothing that falls back.\n\nImplementation\n\nThe layers are checked before the loss because the check is cheaper: loss_seed builds a function, evaluates it at three points and then differentiates and builds again, whereas seeding a layer is one symbolic forward pass. So a chain that cannot be seeded declines before any code is generated at all. The order also decides which reason a model that declines on both counts reports.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.layerwise_gradient_function"},{"category":"method","location":"api/#SymbolicNeuralNetworks.loss_expression-Tuple{AbstractNeuralNetworks.NetworkLoss, Any, Any}","page":"API","text":"loss_expression(loss, ŷ, y)\n\nThe symbolic expression of loss as a function of the prediction ŷ and the target y, from which the layerwise pullback takes its seed partialLpartialhaty.\n\nReturns nothing by default, which means \"not declared\" — the package then guesses the expression with passthrough_expression and checks the guess before using it.\n\nExtending\n\nDeclare the expression for a loss whose relation between prediction and target the guess cannot represent — one that carries extra data of its own, or that compares the prediction to the network's input rather than to output, as an autoencoder loss does:\n\nSymbolicNeuralNetworks.loss_expression(loss::MyLoss, ŷ, y) = ...\n\nA declared expression is used as given. It is deliberately not checked against loss the way the guess is: the reason to declare one is that the four-argument form means something the check would assume it does not, so checking it against that assumption would reject exactly the methods this exists for.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.loss_expression"},{"category":"method","location":"api/#SymbolicNeuralNetworks.loss_seed-Tuple{AbstractNeuralNetworks.NetworkLoss, AbstractSymbolicNeuralNetwork}","page":"API","text":"loss_seed(loss, model; cse, inplace)\n\nBuild the seed of the adjoint sweep: a function (ŷ, y, ps) -> ∂L/∂ŷ.\n\nReturns nothing when the loss cannot be expressed as a function of prediction and target — which is what makes the layerwise construction fall back to the monolithic one instead of computing the wrong thing.\n\nThe expression comes from loss_expression if the loss declares one, and from checked_guess otherwise. Only the guess is checked against loss itself; a declared expression is used as given.\n\nImplementation\n\nps is accepted and ignored: the expression has no parameters, and taking one anyway lets the seed be called exactly like the per-layer kernels of the sweep.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.loss_seed"},{"category":"method","location":"api/#SymbolicNeuralNetworks.monolithic_gradient_function-Tuple{SymbolicNeuralNetwork, AbstractNeuralNetworks.NetworkLoss}","page":"API","text":"monolithic_gradient_function(nn, loss; cse, inplace)\n\nBuild the gradient of loss as one generated function, from one symbolic expression for the loss of the whole network.\n\nThis is what SymbolicPullback used to do unconditionally, and what it still does for a network the layerwise construction does not apply to, or is not worth applying to — see composes_layerwise.\n\nIts cost is the reason for layerwise_gradient_function: the expression is O(width^depth) before anything is differentiated, and differentiating it walks the whole of it once per scalar parameter.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.monolithic_gradient_function"},{"category":"method","location":"api/#SymbolicNeuralNetworks.next_name!-Tuple{Dict{Symbol, Int64}, Symbol}","page":"API","text":"next_name!(counters, name)\n\nReturn the next unused name derived from name and count it in counters.\n\nExamples\n\nusing SymbolicNeuralNetworks: next_name!\n\ncounters = Dict{Symbol, Int}()\n(next_name!(counters, :var), next_name!(counters, :var))\n\n# output\n\n(:var_1, :var_2)\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.next_name!"},{"category":"method","location":"api/#SymbolicNeuralNetworks.parameter_arguments-Tuple{Any}","page":"API","text":"parameter_arguments(sparams)\n\nFlatten a nested parameter set into the flat list of symbolic arrays that Symbolics.build_function is handed, together with the access path of each within the parameter object.\n\nSymbolics.build_function only recognises a symbolic array that is passed to it as an argument, so passing the nested parameter object as a whole would leave its entries as free variables in the generated code. Flattening here and rebuilding the access paths in argument_substitutions keeps the kernel's own interface nested regardless.\n\nExamples\n\nusing SymbolicNeuralNetworks: parameter_arguments, SymbolicNeuralNetwork\nusing AbstractNeuralNetworks: Chain, Dense, params\n\nsnn = SymbolicNeuralNetwork(Chain(Dense(2, 1, tanh)))\nfirst(parameter_arguments(params(snn)))\n\n# output\n\n((:L1, :W), (:L1, :b))\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.parameter_arguments"},{"category":"method","location":"api/#SymbolicNeuralNetworks.passthrough_expression-Tuple{AbstractNeuralNetworks.NetworkLoss, AbstractVector{Symbolics.Num}, AbstractVector{Symbolics.Num}}","page":"API","text":"passthrough_expression(loss, ŷ, y)\n\nGuess the expression of loss as a function of prediction and target, by applying it to a PassThroughLayer — a model whose prediction is its input.\n\nThis is right for every NetworkLoss that reaches its model exactly once, as model(input, ps), and compares the result to output, which is what the losses of AbstractNeuralNetworks do. It is wrong, rather than merely unavailable, for a loss that does something else: an autoencoder loss compares the prediction to the input, and so reads through a pass-through model as identically zero. That is why the guess is checked — see represents_loss — and why loss_expression exists.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.passthrough_expression"},{"category":"method","location":"api/#SymbolicNeuralNetworks.postwalk-Tuple{Any, Any}","page":"API","text":"postwalk(f, x)\n\nApply f to every node of the expression tree x, children first. Nodes that f returns are not visited again, so a rule may insert new syntax without it being rewritten in turn.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.postwalk"},{"category":"method","location":"api/#SymbolicNeuralNetworks.promoted_eltype-Tuple","page":"API","text":"promoted_eltype(args...)\n\nThe element type the generated code will produce, promoted over all inputs.\n\nThis is needed because the in-place kernels write into an array that has to be allocated before they are called, so the element type cannot be taken from a result. Promoting over the inputs keeps Float32 parameters, symbolic (Num) inputs and ForwardDiff.Dual numbers working.\n\nThe walk over a nested parameter set is NeuralNetworkParameters.parameter_eltype, which promotes over the leaves and reaches the storage of a structured one — so a parameter that keeps fewer numbers than its interface shows contributes the element type of the numbers it actually keeps.\n\nNote that this derives the element type from the inputs, not from the expression, which the out-of-place path does instead. The two can differ: an equation over integer inputs and integer parameters evaluates to a Float64, which no Array{Int} can hold, so the allocators widen an integer type with float. A Float32 network whose generated code contains a Float64 constant is rounded to Float32 rather than widened — which is the behaviour one wants for the network, but is worth being aware of.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.promoted_eltype"},{"category":"method","location":"api/#SymbolicNeuralNetworks.reference_parameters-Tuple{AbstractSymbolicNeuralNetwork}","page":"API","text":"reference_parameters(nn)\n\nA numeric parameter set with the shape of nn's symbolic one, filled deterministically.\n\nImplementation\n\nThe shape comes from the symbolic parameters through their NeuralNetworkParameters.ParameterLayout, and the numbers from unflattening a flat vector of them: a layout built over Num leaves unflattens a Float64 vector into Float64 leaves, since a leaf is rebuilt from its prototype's shape and not from its element type. Going through the layout rather than through initialparameters is what keeps this free of the global RNG, and it gets structured parameters right for free.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.reference_parameters"},{"category":"method","location":"api/#SymbolicNeuralNetworks.represents_loss-Tuple{AbstractNeuralNetworks.NetworkLoss, AbstractSymbolicNeuralNetwork, Any}","page":"API","text":"represents_loss(loss, nn, value)\n\nWhether the built passthrough_expression value agrees with loss itself, on nn's model, at a handful of points.\n\ntrue when loss cannot be evaluated in the four-argument numeric form at all, in which case there is nothing to compare against and the expression is taken at its word — that is the case an overriding method for a loss with its own call signature lands in.\n\nImplementation\n\nNeither the parameters (reference_parameters) nor the points are random: whether a pullback builds must not depend on the state of the global RNG, and building one must not advance it — a caller who seeds the RNG and then builds a pullback would otherwise get different data afterwards than before.\n\nThree points, so that an expression which happens to agree at one of them is still rejected. The case in mind is an autoencoder loss, which compares the prediction to the input and so reads as identically zero through a pass-through model — that agrees with the real loss exactly when the real loss is zero too.\n\nThe three differ in direction and not merely in scale: three points on one ray through the origin would leave an expression that is right along that ray and wrong everywhere else undetected, which is no more work to avoid than to allow.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.represents_loss"},{"category":"method","location":"api/#SymbolicNeuralNetworks.scalar_expressions-Tuple{Union{Symbolics.Num, SymbolicUtils.BasicSymbolicImpl.var\"typeof(BasicSymbolicImpl)\"{T} where T, AbstractArray{Symbolics.Num}, AbstractArray{<:SymbolicUtils.BasicSymbolicImpl.var\"typeof(BasicSymbolicImpl)\"{T} where T}}}","page":"API","text":"scalar_expressions(eq)\n\nNormalise an equation to scalar expressions, i.e. to a Num or an Array{Num}.\n\nA Symbolics.Arr — the type @variables x[1:n] produces — is a symbolic object in its own right whose entries are only materialised by Symbolics.scalarize. Symbolics.build_function cannot generate code for one, so every equation passes through here on its way into build_nn_function. Arrays of Num (what this package builds, see symbolic_variables) pass through unchanged.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.scalar_expressions"},{"category":"method","location":"api/#SymbolicNeuralNetworks.seam_arguments-Tuple{AbstractNeuralNetworks.AbstractExplicitLayer, Any}","page":"API","text":"seam_arguments(layer, x)\n\nThe data arguments the kernels generated for layer are called with, given the layer's run-time input x: a tuple in the order (state, carried...) that carried_variables declared. (x,) by default.\n\nExtending\n\nSymbolicNeuralNetworks.seam_arguments(::MyLayer, x::Tuple) = (first(x), flat(last(x)))\n\nThis is the run-time counterpart of seam_value, and the two have to agree: what the seam is written in and what it is called with are the same list. Every entry must have the same rank and batch size as the state. See seam_interface.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.seam_arguments"},{"category":"function","location":"api/#SymbolicNeuralNetworks.seam_interface","page":"API","text":"The seam interface\n\nlayer_seed puts fresh symbolic variables between two layers, which is what keeps the symbolic material a sum over layers rather than a product. By default those variables are one plain vector — the layer's state — because that is what a Dense maps to a Dense.\n\nA layer may carry more than the state. GeometricMachineLearning's SymplecticEuler threads the parameters of the system through the chain alongside it, so it takes and returns a pair. Four functions say how such a layer meets the seam; each defaults to exactly the plain-vector construction, so a layer that carries nothing needs none of them, and a layer that carries something declares all four together:\n\nfunction what it answers default\ncarried_variables what fresh variables the carried data needs ()\nseam_value what the layer is applied to at the seam the state alone\nstate_expressions which part of the output lambda pairs with all of it\nseam_arguments the run-time arguments of the generated kernels the input alone\n\nThe carried data is data, never a differentiation target: lambda pairs with the state, the seed is differentiated with respect to the state and with respect to the layer's parameters, and the carried variables are extra arguments of the generated kernels. So a layer that carries something gets the same two kernels as any other, taking one more argument each.\n\nseam_arguments must return arrays with the same rank and batch size as the state, in the order (state, carried…) that carried_variables declared — the constraint every generated function of this package imposes on its data arguments. For carried data that is the same for the whole batch that means broadcasting it out to one column per sample.\n\ncarried_variables must return variables of its own: layer_seed names the state x and the sensitivities lambda, and a carried array that reuses either name is the same symbolic array rather than a second one. layer_step rejects that rather than letting it through, because nothing downstream would — Symbolics.build_function binds one array to two argument slots and the generated code reads both from the last one, which builds and runs and returns a wrong gradient.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.seam_interface"},{"category":"method","location":"api/#SymbolicNeuralNetworks.seam_value-Tuple{AbstractNeuralNetworks.AbstractExplicitLayer, Any, Vararg{Any}}","page":"API","text":"seam_value(layer, sx, carried...)\n\nWhat layer is applied to at the seam, assembled from the state variables sx and the arrays carried_variables returned. sx alone by default.\n\nExtending\n\nSymbolicNeuralNetworks.seam_value(layer::MyLayer, sx, sc) = (sx, sc)\n\nA layer whose carried data reaches it in some other shape than the flat array the kernels take — SymplecticEuler wants a NamedTuple of system parameters — reassembles it here, and takes it apart again in seam_arguments. See seam_interface.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.seam_value"},{"category":"method","location":"api/#SymbolicNeuralNetworks.split_result-Tuple{NeuralNetworkParameters.ParameterLayout, AbstractVector}","page":"API","text":"split_result(layout, out)\n\nSplit the flat result out of a jointly generated function into the nesting recorded in layout.\n\nout is dispatched on by its number of dimensions, which is how the layout of the batch is accounted for — see AbstractBatchedFunction for where those layouts come from. A vector, which is what a summed batch or a single sample produces, is the case NeuralNetworkParameters.unflatten already covers: every entry simply keeps the shape of its equation. Anything else has a batch dimension and goes to unflatten_batch.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.split_result"},{"category":"method","location":"api/#SymbolicNeuralNetworks.state_expressions-Tuple{AbstractNeuralNetworks.AbstractExplicitLayer, Any}","page":"API","text":"state_expressions(layer, y)\n\nThe part of layer's output lambda pairs with, as scalar expressions. All of it by default.\n\nExtending\n\nSymbolicNeuralNetworks.state_expressions(::MyLayer, y) =\n    SymbolicNeuralNetworks.scalar_expressions(first(y))\n\nA layer that passes its carried data on returns it beside the state, and the seed is lambda_kcdotf_k(x_k-1theta_k) over the state — so this is what says which part that is. See seam_interface.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.state_expressions"},{"category":"method","location":"api/#SymbolicNeuralNetworks.step_parameters-Union{Tuple{Key}, Tuple{SymbolicNeuralNetworks.LayerStep{Key}, Any}} where Key","page":"API","text":"step_parameters(step, ps)\n\nThe entry of the parameter set ps that belongs to step.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.step_parameters"},{"category":"method","location":"api/#SymbolicNeuralNetworks.structured_parameters-Tuple{SymbolicNeuralNetworks.FlatParameterFunction, AbstractVector}","page":"API","text":"structured_parameters(f, w)\n\nThe parameter set w stands for: w laid out according to f's layout, or — when w carries a layout of its own — according to that one.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.structured_parameters"},{"category":"method","location":"api/#SymbolicNeuralNetworks.substitute_symbols-Tuple{Any, Dict{Symbol, Any}}","page":"API","text":"substitute_symbols(expr, substitutions)\n\nReplace every symbol of expr that is a key of substitutions by the corresponding value.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.substitute_symbols"},{"category":"method","location":"api/#SymbolicNeuralNetworks.sweep-Tuple{Tuple, Vararg{Any, 4}}","page":"API","text":"sweep(steps, x, ps, seed, output)\n\nThe derivative of the loss with respect to each step's parameters, as a tuple in the order of steps.\n\nImplementation\n\nThe recursion runs the forward pass on the way in and the adjoint sweep on the way out, so each intermediate result lives on the stack for exactly as long as the backward pass needs it, and the whole sweep stays type stable over the tuple of steps.\n\nThe first step's dλ is never called: it would give the sensitivity of the loss to the network's input, which a parameter gradient has no use for. Hence the two entry points — adjoint_step does the general case, and this function drops that one call. The first step is not built with a dλ at all, so dropping it here is what makes that legal as well as cheaper; see layer_step.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.sweep"},{"category":"method","location":"api/#SymbolicNeuralNetworks.symbolic_derivative-Tuple{Any, AbstractArray}","page":"API","text":"symbolic_derivative(f, differentials)\n\nDifferentiate the scalar expression f with the differential operators in differentials, keeping their shape and nesting. Together with symbolic_differentials this is what turns \"the parameters of a network\" into \"the derivative of f with respect to each of them\".\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.symbolic_derivative"},{"category":"method","location":"api/#SymbolicNeuralNetworks.symbolic_differentials-Tuple{AbstractArray}","page":"API","text":"symbolic_differentials(svariables)\n\nThe differential operators belonging to a set of symbolic variables, with the same shape and nesting as the variables themselves. See symbolic_derivative.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.symbolic_differentials"},{"category":"method","location":"api/#SymbolicNeuralNetworks.symbolic_parameter_gradient-Tuple{Any, AbstractSymbolicNeuralNetwork}","page":"API","text":"symbolic_parameter_gradient(f, nn)\nsymbolic_parameter_gradient(f, sparams)\n\nDifferentiate the symbolic expression f with respect to the parameters of nn, or with respect to a set of symbolic parameters sparams given directly.\n\nThe result has the same nesting as the parameters of nn. For an array-valued f it is an array of such parameter sets, one per entry of f.\n\nThe second form is what the degrees of freedom of an expression that is not a network's forward pass go through: nothing here reads the model, only the parameters. sparams is anything symbolic_differentials can walk, i.e. a NetworkParameters or a nested NamedTuple of symbolic leaves — the same EquationSet the parameters of a SymbolicNeuralNetwork are allowed to be.\n\nThis is used by Gradient and by SymbolicPullback.\n\nExamples\n\nusing SymbolicNeuralNetworks: SymbolicNeuralNetwork, symbolic_parameter_gradient\nusing AbstractNeuralNetworks\nusing AbstractNeuralNetworks: params\n\nc = Chain(Dense(2, 1, tanh))\nnn = SymbolicNeuralNetwork(c)\nsymbolic_parameter_gradient(c(nn.input, params(nn)), nn)[1].L1.b\n\n# output\n\n1-element Vector{Symbolics.Num}:\n 1 - (tanh(W_2₁ + W_1₁ˏ₁*x₁ + W_1₁ˏ₂*x₂)^2)\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.symbolic_parameter_gradient"},{"category":"method","location":"api/#SymbolicNeuralNetworks.symbolic_steps-Tuple{AbstractSymbolicNeuralNetwork}","page":"API","text":"symbolic_steps(nn)\n\nThe sequence of steps nn's model decomposes into, as (layer, key) pairs, or nothing when it does not decompose into one.\n\nA model decomposes when it is a Chain whose layers correspond one-to-one to the entries of the parameter set, and each of whose layers knows the dimensions it maps between — which is what the layerwise pullback needs in order to put fresh variables at the seams. Anything else takes the monolithic path.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.symbolic_steps"},{"category":"method","location":"api/#SymbolicNeuralNetworks.symbolic_variables!-Tuple{Dict{Symbol, Int64}, Real, Symbol}","page":"API","text":"symbolic_variables!(counters, x, name)\n\nThe workhorse of symbolic_variables. counters maps a name to the number of variables that have been created under it so far and is updated in place, which is what makes the leaves of a nested parameter set distinguishable.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.symbolic_variables!"},{"category":"method","location":"api/#SymbolicNeuralNetworks.symbolic_variables-Tuple{Any, Symbol}","page":"API","text":"symbolic_variables(x, name)\n\nBuild symbolic variables with the shape of x, named after name.\n\nx may be a number, an array, or an arbitrarily nested NamedTuple/NetworkParameters of those — i.e. anything that can hold the parameters of a neural network. Every leaf gets its own name, numbered consecutively: name_1, name_2, …\n\nExamples\n\nusing SymbolicNeuralNetworks: symbolic_variables\n\nsymbolic_variables((a = 1.0, b = [1, 2]), :X)\n\n# output\n\n(a = X_1, b = Symbolics.Num[X_2₁, X_2₂])\n\nusing SymbolicNeuralNetworks: symbolic_variables\nusing AbstractNeuralNetworks: NeuralNetwork, Chain, Dense, params\nusing NeuralNetworkParameters: NetworkParameters\n\nnn = NeuralNetwork(Chain(Dense(1, 2; use_bias = false), Dense(2, 1; use_bias = false)))\nsparams = symbolic_variables(params(nn), :W)\n(sparams isa NetworkParameters, keys(sparams), size(sparams.L1.W), eltype(sparams.L1.W))\n\n# output\n\n(true, (:L1, :L2), (2, 1), Symbolics.Num)\n\nImplementation\n\nThe variables are scalar ones (Symbolics.variable/Symbolics.variables), so an array of parameters becomes an Array{Num} rather than a Symbolics.Arr. Symbolics cannot differentiate with respect to the entries of a Symbolics.Arr without scalarising it first, and Symbolics.build_function cannot generate code for expressions that still contain one; using scalar variables throughout avoids both problems. See scalar_expressions.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.symbolic_variables"},{"category":"method","location":"api/#SymbolicNeuralNetworks.trailing_dimensions-Tuple{Tuple{}}","page":"API","text":"trailing_dimensions(equation_size)\n\nThe number of columns one sample of an equation of size equation_size occupies, i.e. the product of all but its first dimension. A scalar-valued equation counts as one of size m = 1, so its empty size gives 1 — Base.tail would throw on it.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.trailing_dimensions"},{"category":"method","location":"api/#SymbolicNeuralNetworks.two_batch_dimension_message-Tuple{Tuple}","page":"API","text":"two_batch_dimension_message(equation_size)\n\nThe error text for an equation whose result already uses the second dimension. Shared with unflatten_batch, so that an entry of an equation set is rejected in the same words as the same equation built on its own.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.two_batch_dimension_message"},{"category":"method","location":"api/#SymbolicNeuralNetworks.unflatten_batch-Tuple{NeuralNetworkParameters.ParametersLayout, AbstractArray}","page":"API","text":"unflatten_batch(layout, out)\n\nSplit a batched flat result into the nesting recorded in layout, giving each entry the shape AbstractBatchedFunction documents for a concatenated batch:\n\na PtimesN matrix, in which an entry of size (m n ldots) becomes an mtimes(ncdotldotscdotN) matrix,\na PtimesN_1timesN_2 array, in which it becomes an mtimesN_1timesN_2 one.\n\nA scalar-valued entry is treated as one of size m = 1 throughout, so it comes back as a 1timesN matrix.\n\nThis is deliberately not a method of NeuralNetworkParameters.unflatten, which already means something else for a matrix: splitting the rows of a Jacobian taken with respect to a flat parameter vector, with no batch dimension to restore.\n\nImplementation\n\nEach entry is copied out of out rather than viewed into it, so that the entries are ordinary Arrays and cannot alias each other.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.unflatten_batch"},{"category":"method","location":"api/#SymbolicNeuralNetworks.unseedable_reason-Tuple{Tuple, Tuple}","page":"API","text":"unseedable_reason(steps, seeds)\n\nThe why decline is given when one of a chain's layers cannot be seeded: the keys and types of every layer whose entry in seeds came back nothing.\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.unseedable_reason"},{"category":"method","location":"api/#SymbolicNeuralNetworks.use_base_mapreduce-Tuple{Any}","page":"API","text":"use_base_mapreduce(expr)\n\nReplace Symbolics._mapreduce with Base.mapreduce.\n\nSymbolics._mapreduce cannot be differentiated by Zygote, whereas Base.mapreduce can. Its trailing Colon(), (:init => false,) arguments are the positional form of dims = Colon().\n\nNothing this package generates contains a Symbolics._mapreduce any more — that used to come from reductions over un-scalarised Symbolics.Arrs, which scalar_expressions now rules out. The rule is kept because the equations a user passes in are not under our control.\n\nExamples\n\nusing SymbolicNeuralNetworks: use_base_mapreduce\n\nuse_base_mapreduce(:(Symbolics._mapreduce(identity, +, x, Colon(), (:init => false,))))\n\n# output\n\n:(mapreduce(identity, +, x; dims = Colon()))\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.use_base_mapreduce"},{"category":"method","location":"api/#SymbolicNeuralNetworks.use_generic_array_constructor-Tuple{Any}","page":"API","text":"use_generic_array_constructor(expr)\n\nReplace SymbolicUtils.Code.create_array(typeof(…), …) with create_array(Array, …).\n\ncreate_array takes the array type to construct as its first argument, and Symbolics fills that in with the type of one of the arguments of the generated function. For us that argument is the parameter set — a NamedTuple, from which no array can be constructed — and even where it is an array it may be a SubArray or a ReshapedArray that create_array has no method for. Array is the generic choice that works in every case.\n\nExamples\n\nusing SymbolicNeuralNetworks: use_generic_array_constructor\n\nuse_generic_array_constructor(:((SymbolicUtils.Code.create_array)(typeof(ps), nothing, Val{1}(), a)))\n\n# output\n\n:(SymbolicUtils.Code.create_array(Array, nothing, Val{1}(), a))\n\n\n\n\n\n","title":"SymbolicNeuralNetworks.use_generic_array_constructor"},{"category":"section","location":"guide/building_functions/#Building-Functions","page":"Building Functions","text":"build_nn_function turns a symbolic expression into an executable Julia function. It is the main entry point of this package, and this page is its reference.\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(3, 4, tanh), Dense(4, 2, tanh))\nsnn = SymbolicNeuralNetwork(c)\nnn = NeuralNetwork(c)\nps = params(nn)\n\nforward = build_nn_function(c(snn.input, params(snn)), snn)","title":"Building Functions"},{"category":"section","location":"guide/building_functions/#Calling-the-result","page":"Building Functions","text":"The generated function takes the data and the parameters:\n\nbuilt_function(input, ps)\nbuilt_function(input, output, ps)     # if the equation also involves a target output\n\nWhich of the two applies is fixed when the function is built — by whether one or two symbolic variable arrays were given:\n\nusing Symbolics\n\nstarget = Symbolics.variables(:y, 1:2)\nresidual = build_nn_function((c(snn.input, params(snn)) - starget) .^ 2, snn, starget)\nresidual(rand(3), rand(2), ps)\n\nThe parameters are passed as one object with the same nesting the symbolic parameters had, which is exactly what AbstractNeuralNetworks.params returns.","title":"Calling the result"},{"category":"section","location":"guide/building_functions/#Batching-and-result-shapes","page":"Building Functions","text":"The data arguments may be a single sample, a batch, or a batch with two batch dimensions:\n\nforward(rand(3), ps)          # a single sample: a vector\n\nforward(rand(3, 5), ps)       # a batch of five: one sample per column\n\nsize(forward(rand(3, 2, 4), ps))   # two batch dimensions\n\nAll data arguments must have the same number of dimensions and the same batch size.\n\nFor an equation of size (m n ldots) and a batch of N samples the result is:\n\ndata arguments reduce result\nvectors either the shape of the equation\ndtimesN matrices hcat mtimes(ncdotldotscdotN)\ndtimesN matrices + the shape of the equation\ndtimesN_1timesN_2 arrays hcat mtimesN_1timesN_2\ndtimesN_1timesN_2 arrays + the shape of the equation\n\nA scalar-valued equation counts as m = 1, so batching it with hcat gives a 1timesN matrix:\n\ntotal = build_nn_function(sum(c(snn.input, params(snn))), snn)\ntotal(rand(3, 5), ps)\n\nA matrix-valued equation batched with hcat places the blocks next to each other — the same layout reduce(hcat, results) would give:\n\nusing SymbolicNeuralNetworks: Jacobian, derivative\n\njacobian = build_nn_function(derivative(Jacobian(snn)), snn)\nsize(jacobian(rand(3, 5), ps))     # 2×3 per sample, five samples\n\nThat layout leaves no room for a second batch dimension, so a matrix-valued equation cannot be evaluated on a three-dimensional batch with reduce = hcat. Use reduce = +, or reshape the input into a matrix.","title":"Batching and result shapes"},{"category":"section","location":"guide/building_functions/#Keyword-arguments","page":"Building Functions","text":"","title":"Keyword arguments"},{"category":"section","location":"guide/building_functions/#reduce","page":"Building Functions","text":"How the results of the individual samples of a batch are combined: hcat (the default) keeps one result per sample, + sums them.\n\nsummed = build_nn_function(c(snn.input, params(snn)), snn; reduce = +)\nsummed(rand(3, 5), ps)\n\nSumming is what a gradient over a batch needs — the derivative of a sum of per-sample terms is the sum of the per-sample derivatives — which is why SymbolicPullback uses it.","title":"reduce"},{"category":"section","location":"guide/building_functions/#inplace","page":"Building Functions","text":"Whether a batch is evaluated with an in-place kernel (the default) or an out-of-place one.\n\nWith inplace = true the result is allocated once and the generated code writes every sample into it, which costs a single allocation per call. The result is produced by mutation, which Zygote does not support:\n\nZygote.gradient(p -> sum(forward(input, p)), ps)   # ERROR: Mutating arrays is not supported\n\nWith inplace = false each sample is evaluated on its own and the results are combined with Base.reduce. That allocates an array per sample but is differentiable:\n\nimport Zygote\n\ndifferentiable = build_nn_function(c(snn.input, params(snn)), snn; inplace = false)\nZygote.gradient(p -> sum(differentiable(rand(3, 5), p)), ps)[1].L1.b\n\nForward-mode AD works either way, as the array the in-place kernel writes into takes its element type from the inputs.\n\nA scalar-valued equation always takes the out-of-place path: Symbolics.build_function emits no in-place form for one.","title":"inplace"},{"category":"section","location":"guide/building_functions/#cse","page":"Building Functions","text":"Whether common subexpression elimination is performed when the code is generated. It is on by default and there is rarely a reason to turn it off.\n\nSymbolics stores an expression as a hash-consed graph but prints it as a tree, so without CSE every reuse of a subexpression is emitted again and the generated code grows exponentially with the depth of the network. With cse = true the shared subexpressions become bindings in a let block instead. For the gradient of a two-hidden-layer network the difference is roughly an order of magnitude in code size; scripts/codegen_comparison.jl measures it.\n\ncse = false produces fully inlined code, which is occasionally useful when inspecting what was generated.","title":"cse"},{"category":"section","location":"guide/building_functions/#What-is-returned","page":"Building Functions","text":"The result is a callable struct, not a closure, so its type is concrete and can be stored in a typed field:\n\ntypeof(forward).name.wrapper\n\nforward\n\nSee InPlaceBatchedFunction and OutOfPlaceBatchedFunction.","title":"What is returned"},{"category":"section","location":"guide/equation_sets/#Equation-Sets","page":"Equation Sets","text":"build_nn_function also accepts a whole set of equations: an arbitrarily nested NamedTuple or NetworkParameters whose leaves are symbolic expressions. The result is one function whose output has the same nesting.\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 3, tanh), Dense(3, 2, tanh))\nsnn = SymbolicNeuralNetwork(c)\nnn = NeuralNetwork(c)\nps = params(nn)\n\neqs = (output = c(snn.input, params(snn)),\n       squared = c(snn.input, params(snn)) .^ 2,\n       total = sum(c(snn.input, params(snn))))\n\nf = build_nn_function(eqs, params(snn), snn.input)\nf([1.0, 2.0], ps)\n\nThis is not just a convenience. The entries are generated as a single function, so everything they have in common is computed once:\n\nf(rand(2, 4), ps)\n\nThe entries follow the shape rules of Building Functions individually — a vector-valued entry becomes a matrix over a batch, a scalar-valued one a 1timesN matrix — and each is copied out of the joint result, so the entries never alias one another.","title":"Equation Sets"},{"category":"section","location":"guide/equation_sets/#Why-a-single-function","page":"Equation Sets","text":"The main use of this is a symbolic gradient, whose entries are the derivatives with respect to each parameter array and therefore all share the entire forward pass:\n\nusing SymbolicNeuralNetworks: symbolic_parameter_gradient\nusing LinearAlgebra: norm\n\ngradient = symbolic_parameter_gradient(norm(c(snn.input, params(snn))) ^ 2, snn)\nkeys(gradient), keys(gradient.L1)\n\nbuild_nn_function(gradient, params(snn), snn.input; reduce = +)(rand(2, 8), ps).L2.W\n\nBuilding each of those four entries as its own function would re-derive the forward pass four times and compile four RuntimeGeneratedFunctions instead of one. Internally the set is flattened into a single vector of scalar equations by flatten_equations and the flat result is split up again by split_result.","title":"Why a single function"},{"category":"section","location":"guide/equation_sets/#Arrays-of-equation-sets","page":"Equation Sets","text":"An array of equation sets — what Gradient produces for an array-valued expression — gives a function returning an array of results. Each entry of the array is built jointly; the entries themselves are independent and stay separate functions.\n\nusing SymbolicNeuralNetworks: Gradient, derivative\n\ng = build_nn_function(derivative(Gradient(snn)), params(snn), snn.input)\nresult = g([1.0, 2.0], ps)\nlength(result), result[1].L1.b","title":"Arrays of equation sets"},{"category":"section","location":"guide/flat_parameters/#Flat-Parameters","page":"Flat Parameters","text":"A solver does not want a parameter set. A Newton iteration, a least-squares fit or a quasi-Newton method wants the unknowns as one vector, and the derivative with respect to that vector as a matrix.\n\nThe conversion between the two shapes is not this package's — it lives in NeuralNetworkParameters, which owns the parameter container itself. flatten copies every number of a parameter set into one vector and hands back the ParameterLayout that puts it together again:\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nusing NeuralNetworkParameters: flatten, unflatten, flatlength\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 3, tanh), Dense(3, 2, tanh))\nnn = NeuralNetwork(c)\nps = params(nn)\n\nw, layout = flatten(ps)\n(flatlength(ps), unflatten(layout, w) == ps)\n\nA layout is a value: built once, then stored in a solver's cache and reused. There are allocation-free flatten!/unflatten! variants for inner loops, ChainRulesCore rules so that reverse mode goes through the conversion, and a FlatParameters wrapper that is an AbstractVector carrying its own layout. See that package's documentation for all of it.\n\nWhat this package adds is the symbolic half: generating a function that takes the flat form, and the derivative with respect to it.","title":"Flat Parameters"},{"category":"section","location":"guide/flat_parameters/#Functions-of-a-flat-parameter-vector","page":"Flat Parameters","text":"build_flat_function is build_nn_function with a flat parameter argument:\n\nsnn = SymbolicNeuralNetwork(c)\nf = build_flat_function(c(snn.input, params(snn)), snn)\n\nf([1.0, 2.0], w) ≈ c([1.0, 2.0], ps)\n\nIt takes the same keyword arguments as build_nn_function and accepts everything it accepts, including an equation set. A FlatParameters works too, and is read through the layout it carries rather than the one the function was built with.\n\nThe vector is laid out with unflatten rather than unflatten!, so it may have a different element type from the parameters the layout was built from — which is what makes the flat form usable for derivatives:\n\nimport ForwardDiff\n\ninput = rand(2)\nForwardDiff.jacobian(v -> f(input, v), w) |> size","title":"Functions of a flat parameter vector"},{"category":"section","location":"guide/flat_parameters/#The-derivative-with-respect-to-the-flat-parameters","page":"Flat Parameters","text":"Asking ForwardDiff for that Jacobian works, but the point of this package is to have it symbolically. flat_parameter_gradient differentiates with respect to the parameters and lays the result out flat — a vector for a scalar expression, and for an array-valued one the mathrmlength(f)timesmathrmflatlength Jacobian with rows indexed by vec(f):\n\nJ = flat_parameter_gradient(c(snn.input, params(snn)), snn)\nsize(J)\n\njacobian = build_nn_function(J, snn)\njacobian(input, ps) ≈ ForwardDiff.jacobian(v -> f(input, v), w)\n\nA column block of that matrix belongs to one entry of the parameter set, and unflatten's matrix method reads it back — the same layout, used for the other of its two meanings:\n\nblocks = unflatten(layout, permutedims(jacobian(input, ps)))\nsize(blocks.L1.W)          # the 6 entries of L1.W against the 2 outputs","title":"The derivative with respect to the flat parameters"},{"category":"section","location":"guide/flat_parameters/#A-nonlinear-solve","page":"Flat Parameters","text":"Put the two together and both directions are flat, which is the shape a Newton step is assembled from — the residual of a network evaluated against a target, and its derivative with respect to the network's degrees of freedom:\n\nusing Symbolics\n\nsoutput = Symbolics.variables(:y, 1:2)\nequation = c(snn.input, params(snn)) - soutput\n\nresidual = build_flat_function(equation, snn, soutput)\njacobian = build_flat_function(flat_parameter_gradient(equation, snn), snn, soutput)\n\nx, y = rand(2), rand(2)\n(residual(x, y, w), size(jacobian(x, y, w)))","title":"A nonlinear solve"},{"category":"section","location":"guide/flat_parameters/#Degrees-of-freedom-that-are-not-a-network's","page":"Flat Parameters","text":"The same pattern works for unknowns that are not the parameters of a network at all. Neither function reads a model, so both take a NetworkParameters of symbolic leaves in place of the symbolic network, and the layout machinery upstream is written against an arbitrarily nested collection of arrays rather than against neural networks — so anything shaped like one flattens.\n\nHere the unknowns are a scale and an offset, and the expression is one no Chain produces:\n\nusing NeuralNetworkParameters: NetworkParameters\n\ndof = NetworkParameters((scale = Symbolics.variables(:s, 1:2),\n                         offset = Symbolics.variables(:o, 1:2, 1:2)))\nsinput = Symbolics.variables(:t, 1:2)\n\nexpression = dof.offset * (dof.scale .* sinput) .- sin.(dof.scale)\n\nr = build_flat_function(expression, dof, sinput)\nJdof = build_flat_function(flat_parameter_gradient(expression, dof), dof, sinput)\nnothing # hide\n\nBoth are then called with the unknowns as one vector, exactly as above:\n\nu, _ = flatten(NetworkParameters((scale = [0.5, 2.0], offset = [1.0 2.0; 3.0 4.0])))\nt = rand(2)\n\n(r(t, u), size(Jdof(t, u)))\n\nflat_parameter_gradient accepts a nested NamedTuple in place of the NetworkParameters as well — it only needs something symbolic_differentials can walk. build_flat_function is the narrower of the two, because build_nn_function dispatches on a NetworkParameters.","title":"Degrees of freedom that are not a network's"},{"category":"section","location":"internals/code_generation/#Code-Generation","page":"Code Generation","text":"How build_nn_function gets from a symbolic expression to a callable object. This page is for maintainers; nothing here is needed to use the package.","title":"Code Generation"},{"category":"section","location":"internals/code_generation/#The-problem","page":"Code Generation","text":"Symbolics.build_function(eq, args...; expression = Val{true}) returns an Expr for a function that\n\ntakes one argument per symbolic array it was given, in that order,\nevaluates the equation for a single sample.\n\nWhat this package needs is a function that\n\ntakes the parameters as one nested object, so that it can be called with AbstractNeuralNetworks.params(nn),\nevaluates one column of a batch, so that a batch costs one allocation rather than one per sample.\n\nBridging that gap is what src/codegen/ does, in four stages.","title":"The problem"},{"category":"section","location":"internals/code_generation/#Stage-1:-ask-Symbolics-for-the-code","page":"Code Generation","text":"generated_expression calls Symbolics.build_function and picks the out-of-place or the in-place half of what it returns. The parameters are handed to it as a flat list of arrays by parameter_arguments, which also records the access path of each within the parameter object:\n\nusing SymbolicNeuralNetworks\nusing SymbolicNeuralNetworks: parameter_arguments\nusing AbstractNeuralNetworks: Chain, Dense, params\n\nc = Chain(Dense(2, 3, tanh), Dense(3, 2, tanh))\nsnn = SymbolicNeuralNetwork(c)\nfirst(parameter_arguments(params(snn)))\n\nFlattening is not optional: Symbolics.build_function only recognises a symbolic array that is passed to it as an argument, so handing it the nested parameter object would leave the entries as free variables in the generated code — which fails at run time with an UndefVarError.\n\nFor a two-layer network the emitted out-of-place code looks like this:\n\nfunction (ˍ₋arg1, ˍ₋arg2, ˍ₋arg3, ˍ₋arg4, ˍ₋arg5)\n    @inbounds begin\n        var\"##cse#1\" = (*)(ˍ₋arg1[2], ˍ₋arg2[4])\n        var\"##cse#2\" = (*)(ˍ₋arg1[1], ˍ₋arg2[1])\n        ⋮\n        (SymbolicUtils.Code.create_array)(typeof(ˍ₋arg1), nothing, Val{1}(), Val{(2,)}(),\n                                          var\"##cse#17\", var\"##cse#22\")\n    end\nend","title":"Stage 1: ask Symbolics for the code"},{"category":"section","location":"internals/code_generation/#Stage-2:-rewrite-it","page":"Code Generation","text":"src/codegen/expression_rewriting.jl turns that into the body of a kernel, with one small rule per problem. All of them work on the syntax tree, via postwalk.\n\nRule What it does\nsubstitute_symbols with argument_substitutions rebinds the generated argument names — by position — to out, x1, x2 and ps.L1.W, ps.L1.b, …\nuse_generic_array_constructor create_array(typeof(…), …) → create_array(Array, …)\nuse_base_mapreduce Symbolics._mapreduce(…) → Base.mapreduce(…; dims = Colon())\nindex_by_batch x1[i] → x1[i, k]\naccumulate_into_output out[i] = … → out[i] += … or out[i + (k-1)·L] = …\n\nafter which the example above reads\n\nfunction (x1, ps, k)\n    @inbounds begin\n        var\"##cse#1\" = (*)(x1[2, k], ps.L1.W[4])\n        var\"##cse#2\" = (*)(x1[1, k], ps.L1.W[1])\n        ⋮\n        (SymbolicUtils.Code.create_array)(Array, nothing, Val{1}(), Val{(2,)}(),\n                                          var\"##cse#17\", var\"##cse#22\")\n    end\nend\n\nA few notes on why each rule is the way it is:\n\nMatching arguments by position is what makes the names irrelevant. It also collapses what used to be two nearly identical pipelines — one for a single data argument, one for two — into one: the number of data arguments is now just a count.\ncreate_array takes the array type to construct as its first argument, and Symbolics fills that in with the type of one of the arguments. That is a parameter array or a data argument, neither of which is the right thing to construct here (SubArray, ReshapedArray, a NamedTuple), so it is replaced by the generic Array.\nindex_by_batch is why the kernels take a batch index at all. It also rejects a data argument that is used other than by reading one entry from it, because the batch dimension would silently be ignored for that use.\naccumulate_into_output relies on the in-place code addressing its output with a single linear index whatever the shape of the equation, which is what makes (k-1)·L the offset of block k in the concatenated result. It insists on exactly one write per entry of the equation: a rule that stopped matching would otherwise leave a kernel that still compiles and runs, but writes every sample of the batch into the same place.\n\nwarning: SymbolicUtils embeds function objects, not symbols\nIn the emitted tree the callee of (getindex)(x, 1) is Base.getindex itself, not the symbol :getindex; typeof(…), on the other hand, is a symbol. A rule that matches only one of the two forms silently does nothing — and in the case of index_by_batch the result is still correct for the first sample of a batch, which makes the bug invisible in a smoke test. callee_name normalises all of the forms (symbol, function object, GlobalRef, qualified path) to a plain Symbol, and every rule goes through it.\n\nThis is also why the rewrites are done on the syntax tree in the first place. They used to be done on the printed form of the code with regexes, which meant a Meta.parse(string(…)) round-trip to normalise (getindex)(x, i) into getindex(x, i), hard-coded ˍ₋argN tokens, and an assertion that the first argument was literally named sinput.","title":"Stage 2: rewrite it"},{"category":"section","location":"internals/code_generation/#Stage-3:-compile-the-kernel","page":"Code Generation","text":"build_kernel and build_kernel! wrap the rewritten body in a function definition and hand it to @RuntimeGeneratedFunction. The resulting signatures are\n\nkernel(x1, ps, k)               # out-of-place\nkernel!(out, x1, ps, k)         # in-place\n\nwith x2 inserted after x1 when there are two data arguments. build_kernel! returns nothing for a scalar-valued equation, for which Symbolics.build_function emits no in-place form; those take the out-of-place path.","title":"Stage 3: compile the kernel"},{"category":"section","location":"internals/code_generation/#Stage-4:-add-the-batching","page":"Code Generation","text":"src/codegen/batched_function.jl wraps the kernel in an AbstractBatchedFunction, which does everything the kernel does not: iterate over the batch, allocate and shape the result, and accept a single sample or a three-dimensional batch instead of a matrix. There is one method per rank of the data arguments, written once for both kernel kinds and both numbers of data arguments.\n\nThese are named structs rather than closures, which makes their type concrete and inferable — the previous implementation returned a local function with several methods, whose return type Julia could not always infer (test/codegen/type_stability.jl guards this).\n\nEquation sets go through one more layer, src/codegen/equation_sets.jl: the set is flattened into a single vector of scalar equations by flatten_equations, built as one function, and the flat result is split up again by split_result.","title":"Stage 4: add the batching"},{"category":"section","location":"internals/code_generation/#Guarding-against-upstream-drift","page":"Code Generation","text":"Every property of the emitted code that the rules depend on is an implementation detail of Symbolics/SymbolicUtils and is asserted directly in test/codegen/codegen_drift.jl:\n\nthe shape of the emitted function and the order of its arguments,\nthat a data argument is only ever read one entry at a time,\nthat create_array still takes a type as its first argument,\nthat the in-place form prepends its output argument and addresses it linearly,\nthat a scalar equation still has no in-place form,\n\neach for cse ∈ (false, true), since with CSE the body becomes a let block of var\"##cse#N\" bindings that the rules have to survive too. Most rules also throw by themselves when they stop matching; the test exists so that drift is reported at its source.","title":"Guarding against upstream drift"},{"category":"section","location":"#SymbolicNeuralNetworks.jl","page":"Home","text":"SymbolicNeuralNetworks builds a symbolic representation of a (small) neural network, lets you form arbitrary expressions from it — derivatives with respect to the input, derivatives with respect to the parameters, and combinations of the two — and compiles those expressions into ordinary Julia functions with RuntimeGeneratedFunctions.\n\nIt is built on AbstractNeuralNetworks and is mostly used together with GeometricMachineLearning and GeometricIntegrators.","title":"SymbolicNeuralNetworks.jl"},{"category":"section","location":"#When-to-reach-for-this-package","page":"Home","text":"The motivation is Zygote's difficulty with second-order derivatives: when a loss function itself contains a derivative of the network — as the losses of Hamiltonian and Lagrangian neural networks do — differentiating it again with reverse-mode AD is either slow or does not work at all. Computing those derivatives symbolically, once, ahead of time, side-steps the problem entirely.\n\nThe trade-off is that the whole network is unrolled into one expression, so this only makes sense for small networks. Code generation time grows with the size of the network, while evaluation is fast and allocates little.","title":"When to reach for this package"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"SymbolicNeuralNetworks\")","title":"Installation"},{"category":"section","location":"#Quickstart","page":"Home","text":"Build a model with AbstractNeuralNetworks, wrap it in a SymbolicNeuralNetwork, and turn symbolic expressions into functions with build_nn_function:\n\nusing SymbolicNeuralNetworks\nusing AbstractNeuralNetworks: Chain, Dense, NeuralNetwork, params\nimport Random\nRandom.seed!(123)\n\nc = Chain(Dense(2, 3, tanh), Dense(3, 1, tanh))\nsnn = SymbolicNeuralNetwork(c)\n\n# the symbolic output of the network, an expression in `snn.input` and `params(snn)`\nsoutput = c(snn.input, params(snn))\n\nforward = build_nn_function(soutput, snn)\n\nnn = NeuralNetwork(c)             # the same model, with numeric parameters\nforward([1.0, 2.0], params(nn))   # a single sample\n\nThe generated function also takes a whole batch, one sample per column:\n\nforward(rand(2, 4), params(nn))\n\nThe derivative with respect to the input is a Jacobian, the derivative with respect to the parameters a Gradient, and both are built into functions the same way:\n\nusing SymbolicNeuralNetworks: Jacobian, derivative\n\njacobian = build_nn_function(derivative(Jacobian(snn)), snn)\njacobian([1.0, 2.0], params(nn))\n\nFor training, SymbolicPullback provides the derivative of a loss with respect to the parameters in the shape an optimizer expects:\n\nusing AbstractNeuralNetworks: FeedForwardLoss\n\npb = SymbolicPullback(snn, FeedForwardLoss())\nloss_value, pullback = pb(params(nn), c, (rand(2, 4), rand(1, 4)))\npullback(1).L1.b","title":"Quickstart"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Symbolic Neural Networks — what a SymbolicNeuralNetwork contains and how to build expressions from it.\nBuilding Functions — build_nn_function in full: batching, result shapes and the cse, inplace and reduce keywords.\nDerivatives — Jacobian, Gradient and SymbolicPullback.\nEquation Sets — building several equations at once.\nFlat Parameters — functions of a flat parameter vector, and the derivative with respect to it, for a solver that wants the network's degrees of freedom as a vector.\nTraining a Symbolic Neural Network — a worked example with GeometricMachineLearning, and how the pullback is built.\nLimitations — the assumptions and rough edges, collected in one place.\nCode Generation — how the generated code is put together, for maintainers.","title":"Where to go next"},{"category":"section","location":"tutorials/double_derivative/#Double-Derivatives","page":"Double Derivatives","text":"SymbolicNeuralNetworks can compute derivatives of arbitrary order, by feeding the symbolic result of one derivative into the next. This is the case Zygote-based AD struggles with, and the reason this package exists.\n\nThe two building blocks are Jacobian (differentiate with respect to the input) and Gradient (differentiate with respect to the parameters); see Derivatives for each on its own.","title":"Double Derivatives"},{"category":"section","location":"tutorials/double_derivative/#Jacobian-of-a-neural-network","page":"Double Derivatives","text":"using AbstractNeuralNetworks\nusing SymbolicNeuralNetworks\nusing SymbolicNeuralNetworks: Jacobian, Gradient, derivative\nusing Latexify: latexify\n\nc = Chain(Dense(2, 1, tanh; use_bias = false))\nnn = SymbolicNeuralNetwork(c)\n□ = Jacobian(nn)\nderivative(□) |> latexify\n\nThe output of nn is one-dimensional and the convention is\n\nsquare_ij = mathrmjacobian_xf_ij = fracpartialpartialx_jf_i\n\nso the result has shape mathrmoutput_dimtimesmathrminput_dim = 1times2:\n\n@assert size(derivative(□)) == (1, 2) # hide\nsize(derivative(□))","title":"Jacobian of a neural network"},{"category":"section","location":"tutorials/double_derivative/#Gradient-of-a-neural-network","page":"Double Derivatives","text":"Gradient differentiates every element of an array-valued expression with respect to the network parameters:\n\ng = Gradient(nn)\n\nderivative(g)[1].L1.W |> latexify","title":"Gradient of a neural network"},{"category":"section","location":"tutorials/double_derivative/#Combining-the-two","page":"Double Derivatives","text":"Feeding the Jacobian into a Gradient differentiates the network twice — first with respect to its input, then with respect to its parameters:\n\ng = Gradient(derivative(□), nn)\nnothing # hide\n\nThe result is a matrix (of the shape of the Jacobian) of parameter sets. To read the derivative of the first Jacobian entry with respect to the weight W of the first layer:\n\nmatrix_index = (1, 1)\nlayer = :L1\nweight = :W\nderivative(g)[matrix_index...][layer][weight] |> latexify\n\nbuild_nn_function turns the whole thing into an executable function. We evaluate it at\n\nx = beginpmatrix 1  0 endpmatrix quad W = beginbmatrix 1  0 endbmatrix\n\nusing AbstractNeuralNetworks: params\nusing NeuralNetworkParameters: NetworkParameters\n\nbuilt_function = build_nn_function(derivative(g), params(nn), nn.input)\n\nx = [1.0, 0.0]\nps = NetworkParameters((L1 = (W = [1.0 0.0],),))\nbuilt_function(x, ps)[matrix_index...][layer][weight]\n\ninfo: Info\nWith Jacobian, Gradient and build_nn_function, combinations of derivatives are just function composition on symbolic expressions. Every entry of the result above is generated as part of a single function, so the forward pass they share is computed once; see Equation Sets.","title":"Combining the two"}]
}
